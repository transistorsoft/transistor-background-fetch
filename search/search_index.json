{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p> React Native </p> <p> Cordova </p> <p> Capacitor </p> <p> Flutter </p>"},{"location":"#background-fetch","title":"Background Fetch","text":"<p>Cross-platform background fetch API for iOS &amp; Android.</p>"},{"location":"capacitor/","title":"Capacitor \u2014 Background Fetch","text":"<p>Cross-platform background fetch API for Capacitor on iOS &amp; Android.</p> <ul> <li> GitHub Repo</li> <li>Setup Guide</li> <li>API Reference \u2014 BackgroundFetch</li> <li>API Reference \u2014 BackgroundFetchConfig</li> <li>API Reference \u2014 TaskConfig</li> </ul>"},{"location":"capacitor/BackgroundFetch/","title":"BackgroundFetch","text":""},{"location":"capacitor/BackgroundFetch/#configure","title":"configure","text":"<pre><code>static configure(config: BackgroundFetchConfig, onEvent: (taskId: string) =&gt; void, onTimeout?: (taskId: string) =&gt; void): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Configure the plugin and begin listening for background-fetch events.</p> <p>Calling <code>configure</code> automatically starts background-fetch (equivalent to calling BackgroundFetch.start immediately after configuration).</p> <p>The BackgroundFetchConfig.minimumFetchInterval option controls how frequently iOS will wake your app in the background.  Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports additional scheduling constraints such as requiredNetworkType, requiresCharging, etc.</p> <p>Warning</p> <p>You must call BackgroundFetch.finish within your event callback.  Failure to do so will result in the OS throttling future background events for your app.</p> <p>Basic configuration</p> <p><pre><code>const status = await BackgroundFetch.configure({\n  minimumFetchInterval: 15,     // &lt;-- iOS: minimum interval in minutes\n}, async (taskId) =&gt; {\n  console.log('[BackgroundFetch] taskId:', taskId);\n  // You MUST call finish() when your work is complete.\n  BackgroundFetch.finish(taskId);\n}, (taskId) =&gt; {\n  // The OS is about to kill your app \u2014 stop all work immediately.\n  console.log('[BackgroundFetch] TIMEOUT, taskId:', taskId);\n  BackgroundFetch.finish(taskId);\n});\n\nconsole.log('[BackgroundFetch] status:', status);\n</code></pre> Android configuration</p> <pre><code>BackgroundFetch.configure({\n  minimumFetchInterval: 15,\n  stopOnTerminate: false,     // &lt;-- Continue after app termination\n  startOnBoot: true,          // &lt;-- Restart on device reboot\n  enableHeadless: true,       // &lt;-- Enable headless task for terminated app\n  requiresCharging: false,\n  requiresBatteryNotLow: true,\n}, async (taskId) =&gt; {\n  console.log('[BackgroundFetch] taskId:', taskId);\n  BackgroundFetch.finish(taskId);\n}, (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"capacitor/BackgroundFetch/#finish","title":"finish","text":"<pre><code>static finish(taskId?: string): void;\n</code></pre> <p>Signal to the OS that your background task is complete.</p> <p>Warning</p> <p>You must call <code>finish</code> at the end of every background-fetch event callback. Failure to call <code>finish</code> has serious consequences:</p> <ul> <li>iOS \u2014 The OS may terminate your app and penalise future background-wake frequency.</li> <li>Android \u2014 The <code>JobScheduler</code> job will not be considered complete and the OS may   throttle or block future executions.</li> </ul> <p>Always call <code>finish</code> as quickly as possible, even when your task encounters an error. For timeout events, call <code>finish</code> immediately without doing any additional work. Example</p> <pre><code>BackgroundFetch.configure({}, async (taskId) =&gt; {\n  try {\n    await doMyWork();\n  } catch (e) {\n    console.warn(e);\n  } finally {\n    // Always call finish, even on error.\n    BackgroundFetch.finish(taskId);\n  }\n}, (taskId) =&gt; {\n  // Timeout \u2014 stop immediately.\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"capacitor/BackgroundFetch/#registerheadlesstask","title":"registerHeadlessTask Android only","text":"<pre><code>static registerHeadlessTask(task: (event: HeadlessEvent) =&gt; Promise&lt;void&gt;): void;\n</code></pre> <p>Register a function to execute when the app is terminated while background-fetch events remain active.</p> <p>Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"capacitor/BackgroundFetch/#how-it-works","title":"How it works","text":"<p>When Android terminates the app, the native module launches a minimal JS context (without the React Native UI) and invokes your headless task function.  This allows you to handle background events even when the app is not running.</p>"},{"location":"capacitor/BackgroundFetch/#registration","title":"Registration","text":"<p>Call <code>registerHeadlessTask</code> at your app's entry point (<code>index.js</code>), outside any React component.  The registration must happen before the OS attempts to fire a headless event.</p>"},{"location":"capacitor/BackgroundFetch/#restrictions","title":"Restrictions","text":"<ul> <li>Do not import modules that depend on the React Native UI runtime (e.g. components,   navigation, Redux stores that render UI).</li> <li>Keep the task lightweight \u2014 the OS may kill the JS context at any time.</li> <li>Always call BackgroundFetch.finish before the returned <code>Promise</code> resolves. Example</li> </ul> <pre><code>// index.js \u2014 register at the app entry point.\nimport BackgroundFetch, { HeadlessEvent } from 'react-native-background-fetch';\n\nconst BackgroundFetchHeadlessTask = async (event: HeadlessEvent) =&gt; {\n  const taskId = event.taskId;\n\n  if (event.timeout) {\n    // OS is about to kill the context \u2014 finish immediately.\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n\n  console.log('[BackgroundFetch] Headless event, taskId:', taskId);\n  // ... perform lightweight work ...\n  BackgroundFetch.finish(taskId);\n};\n\nBackgroundFetch.registerHeadlessTask(BackgroundFetchHeadlessTask);\n</code></pre>"},{"location":"capacitor/BackgroundFetch/#scheduletask","title":"scheduleTask","text":"<pre><code>static scheduleTask(config: TaskConfig): Promise&lt;boolean&gt;;\n</code></pre> <p>Schedule a custom one-shot or periodic background task in addition to the default fetch event registered with BackgroundFetch.configure.</p> <p>Custom tasks fire the same <code>onEvent</code> callback registered in BackgroundFetch.configure, identified by their TaskConfig.taskId.  Call BackgroundFetch.finish with that <code>taskId</code> to signal completion, and BackgroundFetch.stop with the <code>taskId</code> to cancel.</p>"},{"location":"capacitor/BackgroundFetch/#ios","title":"iOS","text":"<p>On iOS, custom tasks are registered using the <code>BGProcessingTask</code> API.  You must declare each <code>taskId</code> in your app's <code>Info.plist</code> under <code>BGTaskSchedulerPermittedIdentifiers</code> before calling <code>scheduleTask</code>.</p>"},{"location":"capacitor/BackgroundFetch/#android","title":"Android","text":"<p>Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports all AbstractConfig scheduling constraints. One-shot task</p> <p><pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.customtask',   // &lt;-- unique identifier\n  delay: 5000,                    // &lt;-- minimum delay in ms\n  forceAlarmManager: true,        // &lt;-- Android: bypass JobScheduler\n  stopOnTerminate: false,\n});\n\n// The same onEvent callback from configure() will fire with taskId 'com.foo.customtask'.\n</code></pre> Periodic task</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.periodic-sync',\n  delay: 600000,       // &lt;-- 10 minutes in ms\n  periodic: true,      // &lt;-- repeat indefinitely\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,  // &lt;-- Android\n  requiresNetworkConnectivity: true,  // &lt;-- iOS\n});\n</code></pre>"},{"location":"capacitor/BackgroundFetch/#start","title":"start","text":"<pre><code>static start(): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Start subscribing to background-fetch events.</p> <p>BackgroundFetch.configure calls <code>start</code> automatically.  Use <code>start</code> explicitly only after a previous call to BackgroundFetch.stop. Example</p> <pre><code>const status = await BackgroundFetch.start();\nconsole.log('[BackgroundFetch] started, status:', status);\n</code></pre>"},{"location":"capacitor/BackgroundFetch/#status","title":"status","text":"<pre><code>static status(callback?: (status: BackgroundFetchStatus) =&gt; void): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Query the current authorization status of the Background Fetch API.</p> Value Constant Description <code>0</code> BackgroundFetchStatus.STATUS_RESTRICTED Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls). <code>1</code> BackgroundFetchStatus.STATUS_DENIED The user explicitly disabled background behavior for this app or the whole system. <code>2</code> BackgroundFetchStatus.STATUS_AVAILABLE Background fetch is available and enabled. Example <pre><code>const status = await BackgroundFetch.status();\nswitch (status) {\n  case BackgroundFetch.STATUS_RESTRICTED:\n    console.log('Background fetch restricted');\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    console.log('Background fetch denied by user');\n    break;\n  case BackgroundFetch.STATUS_AVAILABLE:\n    console.log('Background fetch available');\n    break;\n}\n</code></pre>"},{"location":"capacitor/BackgroundFetch/#stop","title":"stop","text":"<pre><code>static stop(taskId?: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Stop subscribing to background-fetch events.</p> <ul> <li>If a <code>taskId</code> is provided, only that specific scheduled task is cancelled.</li> <li>If no <code>taskId</code> is provided, all background-fetch and scheduled tasks are cancelled.</li> </ul> <p>Use BackgroundFetch.start to resume after stopping. Example</p> <pre><code>// Stop all background-fetch events.\nawait BackgroundFetch.stop();\n\n// Stop a single scheduled task by taskId.\nawait BackgroundFetch.stop('com.foo.customtask');\n</code></pre>"},{"location":"capacitor/BackgroundFetchConfig/","title":"BackgroundFetchConfig","text":"<p>Configuration object passed to BackgroundFetch.configure.</p>"},{"location":"capacitor/BackgroundFetchConfig/#minimumfetchinterval","title":"minimumFetchInterval","text":"<pre><code>minimumFetchInterval?: number;\n</code></pre> <p>The minimum interval in minutes between background-fetch events. Defaults to <code>15</code> minutes.  The minimum allowed value is <code>15</code> minutes.</p> <p>Note</p> <p>The OS does not guarantee fetch events will fire at exactly this interval. iOS adjusts timing based on usage patterns and battery state.  This is a minimum hint, not a precise schedule.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  minimumFetchInterval: 15,   // iOS: fire no more often than every 15 minutes\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"capacitor/BackgroundFetchConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"capacitor/BackgroundFetchConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Set <code>true</code> to enable the headless mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"capacitor/BackgroundFetchConfig/#headless-setup","title":"Headless Setup","text":"<p>With your app open in Android Studio, browse the <code>app</code> folder to find the <code>MainActivity</code> class. Right-click the containing folder and click New &gt; Java Class.</p> <p></p> <p>You MUST name the file <code>BackgroundFetchHeadlessTask</code>.</p> <p></p> <p>Add the following Java code, taking care to preserve your <code>package</code> declaration:</p> <pre><code>package com.your.package.name;  // &lt;-- DO NOT REPLACE THIS LINE\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask {\n    public void onFetch(Context context, BGTask task) {\n        BackgroundFetch backgroundFetch = BackgroundFetch.getInstance(context);\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            backgroundFetch.finish(taskId);\n            return;\n        }\n        Log.d(\"MyHeadlessTask\", \"onFetch: \" + taskId);\n        // Do your work here...\n\n        backgroundFetch.finish(taskId);\n    }\n}\n</code></pre>"},{"location":"capacitor/BackgroundFetchConfig/#testing-your-headless-task","title":"Testing Your Headless Task","text":"<p>After terminating your app, simulate a fetch event while observing <code>$ adb logcat</code>:</p> <pre><code>adb shell cmd jobscheduler run -f com.your.app.package.name 999\n</code></pre>"},{"location":"capacitor/BackgroundFetchConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>forceAlarmManager?: boolean;\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"capacitor/BackgroundFetchConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>requiredNetworkType?: NetworkType;\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n});\n</code></pre>"},{"location":"capacitor/BackgroundFetchConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>requiresBatteryNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"capacitor/BackgroundFetchConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>requiresCharging?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"capacitor/BackgroundFetchConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>requiresDeviceIdle?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"capacitor/BackgroundFetchConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>requiresStorageNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"capacitor/BackgroundFetchConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"capacitor/BackgroundFetchConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"capacitor/BackgroundFetchStatus/","title":"BackgroundFetchStatus","text":"<p>Authorization status returned by BackgroundFetch.status and BackgroundFetch.configure.</p>"},{"location":"capacitor/BackgroundFetchStatus/#status_available","title":"STATUS_AVAILABLE","text":"<pre><code>static STATUS_AVAILABLE: BackgroundFetchStatus = 2;\n</code></pre> <p>Background fetch is available and enabled.</p>"},{"location":"capacitor/BackgroundFetchStatus/#status_denied","title":"STATUS_DENIED","text":"<pre><code>static STATUS_DENIED: BackgroundFetchStatus = 1;\n</code></pre> <p>The user explicitly disabled background behavior for this app or for the whole system.</p>"},{"location":"capacitor/BackgroundFetchStatus/#status_restricted","title":"STATUS_RESTRICTED","text":"<pre><code>static STATUS_RESTRICTED: BackgroundFetchStatus = 0;\n</code></pre> <p>Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls are in effect).</p>"},{"location":"capacitor/HeadlessEvent/","title":"HeadlessEvent","text":"<p>The event object delivered to a headless task handler when the app is running in a terminated (headless) state on Android.</p> <p>With your app open in Android Studio, browse the <code>app</code> folder to find the <code>MainActivity</code> class. Right-click the containing folder and click New &gt; Java Class.</p> <p></p> <p>You MUST name the file <code>BackgroundFetchHeadlessTask</code>.</p> <p></p> <p>Add the following Java code, taking care to preserve your <code>package</code> declaration:</p> <pre><code>package com.your.package.name;  // &lt;-- DO NOT REPLACE THIS LINE\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask {\n    public void onFetch(Context context, BGTask task) {\n        BackgroundFetch backgroundFetch = BackgroundFetch.getInstance(context);\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            backgroundFetch.finish(taskId);\n            return;\n        }\n        Log.d(\"MyHeadlessTask\", \"onFetch: \" + taskId);\n        // Do your work here...\n\n        backgroundFetch.finish(taskId);\n    }\n}\n</code></pre>"},{"location":"capacitor/HeadlessEvent/#taskid","title":"taskId","text":"<pre><code>taskId: string;\n</code></pre> <p>The identifier of the task that fired this headless event.</p> <p>Pass this value to BackgroundFetch.finish when your work is complete.</p>"},{"location":"capacitor/HeadlessEvent/#timeout","title":"timeout","text":"<pre><code>timeout: boolean;\n</code></pre> <p><code>true</code> when the OS signals that background running-time is nearly exhausted.</p> <p>When a timeout event fires you must immediately call BackgroundFetch.finish and stop any in-progress work.  Failure to do so may cause the OS to penalize your app.</p>"},{"location":"capacitor/NetworkType/","title":"NetworkType","text":"<p>Android only Network connectivity constraint for use with requiredNetworkType.</p>"},{"location":"capacitor/NetworkType/#network_type_any","title":"NETWORK_TYPE_ANY","text":"<pre><code>static NETWORK_TYPE_ANY: NetworkType = 1;\n</code></pre> <p>Requires any active network connection.</p>"},{"location":"capacitor/NetworkType/#network_type_cellular","title":"NETWORK_TYPE_CELLULAR","text":"<pre><code>static NETWORK_TYPE_CELLULAR: NetworkType = 2;\n</code></pre> <p>Requires a cellular (mobile data) network connection.</p>"},{"location":"capacitor/NetworkType/#network_type_none","title":"NETWORK_TYPE_NONE","text":"<pre><code>static NETWORK_TYPE_NONE: NetworkType = 0;\n</code></pre> <p>No network constraint \u2014 the task will run regardless of connectivity.  This is the default.</p>"},{"location":"capacitor/NetworkType/#network_type_not_roaming","title":"NETWORK_TYPE_NOT_ROAMING","text":"<pre><code>static NETWORK_TYPE_NOT_ROAMING: NetworkType = 4;\n</code></pre> <p>Requires a non-roaming network connection.</p>"},{"location":"capacitor/NetworkType/#network_type_unmetered","title":"NETWORK_TYPE_UNMETERED","text":"<pre><code>static NETWORK_TYPE_UNMETERED: NetworkType = 3;\n</code></pre> <p>Requires an unmetered network connection (e.g. Wi-Fi).</p>"},{"location":"capacitor/TaskConfig/","title":"TaskConfig","text":"<p>Configuration object passed to BackgroundFetch.scheduleTask.</p>"},{"location":"capacitor/TaskConfig/#delay","title":"delay","text":"<pre><code>delay: number;\n</code></pre> <p>The minimum delay in milliseconds before this task fires.</p> <p>The OS may delay the task beyond this value depending on device conditions, battery state, and other scheduling constraints. Example</p> <pre><code>// Fire at least 30 seconds from now.\nBackgroundFetch.scheduleTask({\n  taskId: 'com.foo.task',\n  delay: 30000,\n});\n</code></pre>"},{"location":"capacitor/TaskConfig/#periodic","title":"periodic","text":"<pre><code>periodic?: boolean;\n</code></pre> <p>Set <code>true</code> to schedule a repeating task.  Defaults to <code>false</code> (one-shot).</p> <p>When <code>true</code>, the task automatically re-schedules itself after each execution using the same TaskConfig.delay as the repeat interval. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.periodic',\n  delay: 900000,    // 15 minutes in ms\n  periodic: true,\n});\n</code></pre>"},{"location":"capacitor/TaskConfig/#requiresnetworkconnectivity","title":"requiresNetworkConnectivity iOS only","text":"<pre><code>requiresNetworkConnectivity?: boolean;\n</code></pre> <p>Set <code>true</code> to require a network connection before this task runs.</p> <p>On Android, use requiredNetworkType instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.networktask',\n  delay: 60000,\n  requiresNetworkConnectivity: true,      // &lt;-- iOS\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY, // &lt;-- Android\n});\n</code></pre>"},{"location":"capacitor/TaskConfig/#taskid","title":"taskId","text":"<pre><code>taskId: string;\n</code></pre> <p>A unique identifier for this task.</p> <p>The same <code>taskId</code> is delivered to the <code>onEvent</code> callback registered in BackgroundFetch.configure.  Pass it to BackgroundFetch.finish to signal completion and to BackgroundFetch.stop to cancel the task.</p> <p>Use reverse-domain notation to avoid collisions with other tasks. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.myapp.sync',\n  delay: 60000,\n});\n\n// In your configure() callback:\nBackgroundFetch.configure({}, async (taskId) =&gt; {\n  if (taskId === 'com.myapp.sync') {\n    await performSync();\n  }\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"capacitor/TaskConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"capacitor/TaskConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Set <code>true</code> to enable the headless mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"capacitor/TaskConfig/#headless-setup","title":"Headless Setup","text":"<p>With your app open in Android Studio, browse the <code>app</code> folder to find the <code>MainActivity</code> class. Right-click the containing folder and click New &gt; Java Class.</p> <p></p> <p>You MUST name the file <code>BackgroundFetchHeadlessTask</code>.</p> <p></p> <p>Add the following Java code, taking care to preserve your <code>package</code> declaration:</p> <pre><code>package com.your.package.name;  // &lt;-- DO NOT REPLACE THIS LINE\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask {\n    public void onFetch(Context context, BGTask task) {\n        BackgroundFetch backgroundFetch = BackgroundFetch.getInstance(context);\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            backgroundFetch.finish(taskId);\n            return;\n        }\n        Log.d(\"MyHeadlessTask\", \"onFetch: \" + taskId);\n        // Do your work here...\n\n        backgroundFetch.finish(taskId);\n    }\n}\n</code></pre>"},{"location":"capacitor/TaskConfig/#testing-your-headless-task","title":"Testing Your Headless Task","text":"<p>After terminating your app, simulate a fetch event while observing <code>$ adb logcat</code>:</p> <pre><code>adb shell cmd jobscheduler run -f com.your.app.package.name 999\n</code></pre>"},{"location":"capacitor/TaskConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>forceAlarmManager?: boolean;\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"capacitor/TaskConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>requiredNetworkType?: NetworkType;\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n});\n</code></pre>"},{"location":"capacitor/TaskConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>requiresBatteryNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"capacitor/TaskConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>requiresCharging?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"capacitor/TaskConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>requiresDeviceIdle?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"capacitor/TaskConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>requiresStorageNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"capacitor/TaskConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"capacitor/TaskConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"capacitor/debugging/","title":"Debugging","text":"<p>Background Fetch events are notoriously difficult to test because the OS controls when they fire.  Use these simulation techniques during development.</p>"},{"location":"capacitor/debugging/#ios","title":"iOS","text":""},{"location":"capacitor/debugging/#simulating-fetch-events","title":"Simulating Fetch Events","text":"<p>iOS 13+ uses the <code>BGTaskScheduler</code> API.  To simulate a fetch event:</p> <ol> <li>Run your app in Xcode.</li> <li>Click the Pause button () to initiate a breakpoint.</li> <li>In the <code>(lldb)</code> console, paste:</li> </ol> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <ol> <li>Click the Continue button ().  The callback provided to <code>configure</code> will receive the event.</li> </ol> <p></p> <p></p> <p></p>"},{"location":"capacitor/debugging/#simulating-custom-scheduletask-events","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<p>If you've registered a custom task (e.g. <code>com.transistorsoft.customtask</code>), simulate it with:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.customtask\"]\n</code></pre>"},{"location":"capacitor/debugging/#simulating-task-timeout-events","title":"Simulating Task Timeout Events","text":"<p>To simulate a task timeout, do not call <code>finish(taskId)</code> in your event callback, then run:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateExpirationForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <p>Your timeout callback will fire, allowing you to verify your timeout handling logic.</p>"},{"location":"capacitor/debugging/#ios-tips","title":"iOS Tips","text":"<p>Warning</p> <p>iOS can take hours or even days before Apple's machine-learning algorithm begins regularly firing fetch events.  Do not sit staring at your logs waiting for an event.  If your simulated events work, everything is correctly configured.</p> <ul> <li>If the user doesn't open your app for long periods of time, iOS will stop firing events.</li> <li><code>scheduleTask</code> on iOS seems only to run when the device is plugged into power.</li> <li>There is no <code>stopOnTerminate: false</code> for iOS \u2014 when the app is terminated, iOS stops firing events.</li> </ul>"},{"location":"capacitor/debugging/#android","title":"Android","text":""},{"location":"capacitor/debugging/#observing-logs","title":"Observing Logs","text":"<p>Use <code>adb logcat</code> to observe plugin logs:</p> <pre><code>adb logcat \"*:S\" TSBackgroundFetch:V\n</code></pre>"},{"location":"capacitor/debugging/#simulating-fetch-events_1","title":"Simulating Fetch Events","text":"<p>Simulate a background-fetch event (replace <code>&lt;your.application.id&gt;</code> with your app's package name):</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre>"},{"location":"capacitor/debugging/#simulating-custom-scheduletask-events_1","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<ol> <li>Observe <code>adb logcat</code> for the <code>registerTask</code> log entry and copy the <code>jobId</code>:</li> </ol> <pre><code>TSBackgroundFetch: - registerTask: com.your.package.name (jobId: -359368280)\n</code></pre> <ol> <li>Paste the <code>jobId</code> into the <code>adb shell</code> command:</li> </ol> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; -359368280\n</code></pre>"},{"location":"capacitor/debugging/#simulating-headless-events","title":"Simulating Headless Events","text":"<p>After terminating your app, simulate a fetch event to test your headless task:</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre> <p>Verify the headless task executes by observing <code>adb logcat</code> output.</p>"},{"location":"capacitor/examples/","title":"Examples","text":""},{"location":"capacitor/examples/#basic-usage","title":"Basic Usage","text":"<pre><code>import {BackgroundFetch} from '@transistorsoft/capacitor-background-fetch';\n\nconst status = await BackgroundFetch.configure(\n  {\n    minimumFetchInterval: 15, // minutes\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  async (taskId: string) =&gt; {\n    // &lt;-- Event callback\n    console.log(\"[BackgroundFetch] Event received:\", taskId);\n    // Perform your work here...\n    BackgroundFetch.finish(taskId);\n  },\n  async (taskId: string) =&gt; {\n    // &lt;-- Timeout callback\n    console.log(\"[BackgroundFetch] TIMEOUT:\", taskId);\n    BackgroundFetch.finish(taskId);\n  }\n);\nconsole.log(\"[BackgroundFetch] configure status:\", status);\n</code></pre>"},{"location":"capacitor/examples/#scheduling-a-custom-task","title":"Scheduling a Custom Task","text":"<pre><code>BackgroundFetch.scheduleTask({\n  taskId: \"com.example.my-custom-task\",\n  delay: 60000, // milliseconds\n  periodic: false,\n  forceAlarmManager: true,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,\n});\n</code></pre> <p>All scheduled tasks fire into the same callback provided to <code>configure</code>.  Use a <code>switch</code> statement to route by <code>taskId</code>:</p> <pre><code>const status = await BackgroundFetch.configure(\n  { minimumFetchInterval: 15 },\n  async (taskId: string) =&gt; {\n    switch (taskId) {\n      case \"com.example.my-custom-task\":\n        console.log(\"Custom task fired\");\n        break;\n      default:\n        console.log(\"Default fetch event\");\n    }\n    BackgroundFetch.finish(taskId);\n  },\n  async (taskId: string) =&gt; {\n    BackgroundFetch.finish(taskId);\n  }\n);\n</code></pre>"},{"location":"capacitor/examples/#headless-task-android","title":"Headless Task (Android)","text":"<p>When the app is terminated, Android can continue executing background-fetch events via a custom Java class.</p> <p>With your app open in Android Studio, browse the <code>app</code> folder to find the <code>MainActivity</code> class.  Right-click the containing folder and click New &gt; Java Class.</p> <p>You MUST name the file <code>BackgroundFetchHeadlessTask</code>:</p> <pre><code>package com.your.package.name;  // &lt;-- DO NOT REPLACE THIS LINE\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask {\n    public void onFetch(Context context, BGTask task) {\n        BackgroundFetch backgroundFetch = BackgroundFetch.getInstance(context);\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            backgroundFetch.finish(taskId);\n            return;\n        }\n        Log.d(\"MyHeadlessTask\", \"onFetch: \" + taskId);\n        // Do your work here...\n\n        backgroundFetch.finish(taskId);\n    }\n}\n</code></pre> <p>Note</p> <p>See enableHeadless for full setup details. Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"capacitor/examples/#checking-status","title":"Checking Status","text":"<pre><code>const status = await BackgroundFetch.status;\n\nswitch (status) {\n  case BackgroundFetch.STATUS_AVAILABLE:\n    console.log(\"Background fetch is available\");\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    console.log(\"Background fetch is denied by the user\");\n    break;\n  case BackgroundFetch.STATUS_RESTRICTED:\n    console.log(\"Background fetch is restricted (e.g. parental controls)\");\n    break;\n}\n</code></pre>"},{"location":"capacitor/examples/#stopping-tasks","title":"Stopping Tasks","text":"<pre><code>// Stop a specific scheduled task\nawait BackgroundFetch.stop(\"com.example.my-custom-task\");\n\n// Stop ALL tasks (including the default fetch event)\nawait BackgroundFetch.stop();\n</code></pre>"},{"location":"capacitor/setup/","title":"Capacitor \u2014 Setup","text":""},{"location":"capacitor/setup/#installation","title":"Installation","text":"npmyarn <pre><code>npm install @transistorsoft/capacitor-background-fetch\nnpx cap sync\n</code></pre> <pre><code>yarn add @transistorsoft/capacitor-background-fetch\nnpx cap sync\n</code></pre>"},{"location":"capacitor/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"capacitor/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your project root \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Background fetch</li> <li>[x] Background processing (only if you intend to use <code>BackgroundFetch.scheduleTask</code>)</li> </ul> <p></p>"},{"location":"capacitor/setup/#infoplist","title":"Info.plist","text":"<p>Open your Info.plist and add the key \"Permitted background task scheduler identifiers\":</p> <p></p> <p>Add the required identifier <code>com.transistorsoft.fetch</code>:</p> <p></p> <p>If you intend to execute your own custom tasks via <code>BackgroundFetch.scheduleTask</code>, you must add those custom identifiers as well.  For example, if you intend to execute a custom <code>taskId: 'com.transistorsoft.customtask'</code>, you must add the identifier <code>com.transistorsoft.customtask</code> to your \"Permitted background task scheduler identifiers\", as well.</p> <p>Warning</p> <p>A task identifier can be any string you wish, but it must be prefixed with <code>com.transistorsoft.</code>.</p>"},{"location":"capacitor/setup/#android-setup","title":"Android Setup","text":"<p>No additional Android setup is required.</p>"},{"location":"cordova/","title":"Cordova \u2014 Background Fetch","text":"<p>Cross-platform background fetch API for Cordova on iOS &amp; Android.</p> <ul> <li> GitHub Repo</li> <li>Setup Guide</li> <li>API Reference \u2014 BackgroundFetch</li> <li>API Reference \u2014 BackgroundFetchConfig</li> <li>API Reference \u2014 TaskConfig</li> </ul>"},{"location":"cordova/BackgroundFetch/","title":"BackgroundFetch","text":""},{"location":"cordova/BackgroundFetch/#configure","title":"configure","text":"<pre><code>static configure(config: BackgroundFetchConfig, onEvent: (taskId: string) =&gt; void, onTimeout?: (taskId: string) =&gt; void): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Configure the plugin and begin listening for background-fetch events.</p> <p>Calling <code>configure</code> automatically starts background-fetch (equivalent to calling BackgroundFetch.start immediately after configuration).</p> <p>The BackgroundFetchConfig.minimumFetchInterval option controls how frequently iOS will wake your app in the background.  Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports additional scheduling constraints such as requiredNetworkType, requiresCharging, etc.</p> <p>Warning</p> <p>You must call BackgroundFetch.finish within your event callback.  Failure to do so will result in the OS throttling future background events for your app.</p> <p>Basic configuration</p> <p><pre><code>const status = await BackgroundFetch.configure({\n  minimumFetchInterval: 15,     // &lt;-- iOS: minimum interval in minutes\n}, async (taskId) =&gt; {\n  console.log('[BackgroundFetch] taskId:', taskId);\n  // You MUST call finish() when your work is complete.\n  BackgroundFetch.finish(taskId);\n}, (taskId) =&gt; {\n  // The OS is about to kill your app \u2014 stop all work immediately.\n  console.log('[BackgroundFetch] TIMEOUT, taskId:', taskId);\n  BackgroundFetch.finish(taskId);\n});\n\nconsole.log('[BackgroundFetch] status:', status);\n</code></pre> Android configuration</p> <pre><code>BackgroundFetch.configure({\n  minimumFetchInterval: 15,\n  stopOnTerminate: false,     // &lt;-- Continue after app termination\n  startOnBoot: true,          // &lt;-- Restart on device reboot\n  enableHeadless: true,       // &lt;-- Enable headless task for terminated app\n  requiresCharging: false,\n  requiresBatteryNotLow: true,\n}, async (taskId) =&gt; {\n  console.log('[BackgroundFetch] taskId:', taskId);\n  BackgroundFetch.finish(taskId);\n}, (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"cordova/BackgroundFetch/#finish","title":"finish","text":"<pre><code>static finish(taskId?: string): void;\n</code></pre> <p>Signal to the OS that your background task is complete.</p> <p>Warning</p> <p>You must call <code>finish</code> at the end of every background-fetch event callback. Failure to call <code>finish</code> has serious consequences:</p> <ul> <li>iOS \u2014 The OS may terminate your app and penalise future background-wake frequency.</li> <li>Android \u2014 The <code>JobScheduler</code> job will not be considered complete and the OS may   throttle or block future executions.</li> </ul> <p>Always call <code>finish</code> as quickly as possible, even when your task encounters an error. For timeout events, call <code>finish</code> immediately without doing any additional work. Example</p> <pre><code>BackgroundFetch.configure({}, async (taskId) =&gt; {\n  try {\n    await doMyWork();\n  } catch (e) {\n    console.warn(e);\n  } finally {\n    // Always call finish, even on error.\n    BackgroundFetch.finish(taskId);\n  }\n}, (taskId) =&gt; {\n  // Timeout \u2014 stop immediately.\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"cordova/BackgroundFetch/#registerheadlesstask","title":"registerHeadlessTask Android only","text":"<pre><code>static registerHeadlessTask(task: (event: HeadlessEvent) =&gt; Promise&lt;void&gt;): void;\n</code></pre> <p>Register a function to execute when the app is terminated while background-fetch events remain active.</p> <p>Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"cordova/BackgroundFetch/#how-it-works","title":"How it works","text":"<p>When Android terminates the app, the native module launches a minimal JS context (without the React Native UI) and invokes your headless task function.  This allows you to handle background events even when the app is not running.</p>"},{"location":"cordova/BackgroundFetch/#registration","title":"Registration","text":"<p>Call <code>registerHeadlessTask</code> at your app's entry point (<code>index.js</code>), outside any React component.  The registration must happen before the OS attempts to fire a headless event.</p>"},{"location":"cordova/BackgroundFetch/#restrictions","title":"Restrictions","text":"<ul> <li>Do not import modules that depend on the React Native UI runtime (e.g. components,   navigation, Redux stores that render UI).</li> <li>Keep the task lightweight \u2014 the OS may kill the JS context at any time.</li> <li>Always call BackgroundFetch.finish before the returned <code>Promise</code> resolves. Example</li> </ul> <pre><code>// index.js \u2014 register at the app entry point.\nimport BackgroundFetch, { HeadlessEvent } from 'react-native-background-fetch';\n\nconst BackgroundFetchHeadlessTask = async (event: HeadlessEvent) =&gt; {\n  const taskId = event.taskId;\n\n  if (event.timeout) {\n    // OS is about to kill the context \u2014 finish immediately.\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n\n  console.log('[BackgroundFetch] Headless event, taskId:', taskId);\n  // ... perform lightweight work ...\n  BackgroundFetch.finish(taskId);\n};\n\nBackgroundFetch.registerHeadlessTask(BackgroundFetchHeadlessTask);\n</code></pre>"},{"location":"cordova/BackgroundFetch/#scheduletask","title":"scheduleTask","text":"<pre><code>static scheduleTask(config: TaskConfig): Promise&lt;boolean&gt;;\n</code></pre> <p>Schedule a custom one-shot or periodic background task in addition to the default fetch event registered with BackgroundFetch.configure.</p> <p>Custom tasks fire the same <code>onEvent</code> callback registered in BackgroundFetch.configure, identified by their TaskConfig.taskId.  Call BackgroundFetch.finish with that <code>taskId</code> to signal completion, and BackgroundFetch.stop with the <code>taskId</code> to cancel.</p>"},{"location":"cordova/BackgroundFetch/#ios","title":"iOS","text":"<p>On iOS, custom tasks are registered using the <code>BGProcessingTask</code> API.  You must declare each <code>taskId</code> in your app's <code>Info.plist</code> under <code>BGTaskSchedulerPermittedIdentifiers</code> before calling <code>scheduleTask</code>.</p>"},{"location":"cordova/BackgroundFetch/#android","title":"Android","text":"<p>Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports all AbstractConfig scheduling constraints. One-shot task</p> <p><pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.customtask',   // &lt;-- unique identifier\n  delay: 5000,                    // &lt;-- minimum delay in ms\n  forceAlarmManager: true,        // &lt;-- Android: bypass JobScheduler\n  stopOnTerminate: false,\n});\n\n// The same onEvent callback from configure() will fire with taskId 'com.foo.customtask'.\n</code></pre> Periodic task</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.periodic-sync',\n  delay: 600000,       // &lt;-- 10 minutes in ms\n  periodic: true,      // &lt;-- repeat indefinitely\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,  // &lt;-- Android\n  requiresNetworkConnectivity: true,  // &lt;-- iOS\n});\n</code></pre>"},{"location":"cordova/BackgroundFetch/#start","title":"start","text":"<pre><code>static start(): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Start subscribing to background-fetch events.</p> <p>BackgroundFetch.configure calls <code>start</code> automatically.  Use <code>start</code> explicitly only after a previous call to BackgroundFetch.stop. Example</p> <pre><code>const status = await BackgroundFetch.start();\nconsole.log('[BackgroundFetch] started, status:', status);\n</code></pre>"},{"location":"cordova/BackgroundFetch/#status","title":"status","text":"<pre><code>static status(callback?: (status: BackgroundFetchStatus) =&gt; void): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Query the current authorization status of the Background Fetch API.</p> Value Constant Description <code>0</code> BackgroundFetchStatus.STATUS_RESTRICTED Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls). <code>1</code> BackgroundFetchStatus.STATUS_DENIED The user explicitly disabled background behavior for this app or the whole system. <code>2</code> BackgroundFetchStatus.STATUS_AVAILABLE Background fetch is available and enabled. Example <pre><code>const status = await BackgroundFetch.status();\nswitch (status) {\n  case BackgroundFetch.STATUS_RESTRICTED:\n    console.log('Background fetch restricted');\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    console.log('Background fetch denied by user');\n    break;\n  case BackgroundFetch.STATUS_AVAILABLE:\n    console.log('Background fetch available');\n    break;\n}\n</code></pre>"},{"location":"cordova/BackgroundFetch/#stop","title":"stop","text":"<pre><code>static stop(taskId?: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Stop subscribing to background-fetch events.</p> <ul> <li>If a <code>taskId</code> is provided, only that specific scheduled task is cancelled.</li> <li>If no <code>taskId</code> is provided, all background-fetch and scheduled tasks are cancelled.</li> </ul> <p>Use BackgroundFetch.start to resume after stopping. Example</p> <pre><code>// Stop all background-fetch events.\nawait BackgroundFetch.stop();\n\n// Stop a single scheduled task by taskId.\nawait BackgroundFetch.stop('com.foo.customtask');\n</code></pre>"},{"location":"cordova/BackgroundFetchConfig/","title":"BackgroundFetchConfig","text":"<p>Configuration object passed to BackgroundFetch.configure.</p>"},{"location":"cordova/BackgroundFetchConfig/#minimumfetchinterval","title":"minimumFetchInterval","text":"<pre><code>minimumFetchInterval?: number;\n</code></pre> <p>The minimum interval in minutes between background-fetch events. Defaults to <code>15</code> minutes.  The minimum allowed value is <code>15</code> minutes.</p> <p>Note</p> <p>The OS does not guarantee fetch events will fire at exactly this interval. iOS adjusts timing based on usage patterns and battery state.  This is a minimum hint, not a precise schedule.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  minimumFetchInterval: 15,   // iOS: fire no more often than every 15 minutes\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"cordova/BackgroundFetchConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"cordova/BackgroundFetchConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Set <code>true</code> to enable the headless mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p> <p>Warning</p> <p>You must be prepared to write Java code.</p>"},{"location":"cordova/BackgroundFetchConfig/#headless-setup","title":"Headless Setup","text":"<p>Step 1: Create a file named <code>BackgroundFetchHeadlessTask.java</code> in your app (e.g. <code>src/android/</code>). The file can be located anywhere but MUST be named <code>BackgroundFetchHeadlessTask.java</code>:</p> <pre><code>package com.transistorsoft.cordova.backgroundfetch;\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask implements HeadlessTask {\n    @Override\n    public void onFetch(Context context, BGTask task) {\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            Log.d(BackgroundFetch.TAG, \"TIMEOUT: \" + taskId);\n            BackgroundFetch.getInstance(context).finish(taskId);\n            return;\n        }\n        Log.d(BackgroundFetch.TAG, \"onFetch: \" + taskId);\n        // Perform your work here...\n\n        BackgroundFetch.getInstance(context).finish(taskId);\n    }\n}\n</code></pre> <p>Step 2: In your <code>config.xml</code>, add a <code>&lt;resource-file&gt;</code> within <code>&lt;platform name=\"android\"&gt;</code>:</p> <pre><code>&lt;platform name=\"android\"&gt;\n    &lt;resource-file\n        src=\"src/android/BackgroundFetchHeadlessTask.java\"\n        target=\"app/src/main/java/com/transistorsoft/cordova/backgroundfetch/BackgroundFetchHeadlessTask.java\" /&gt;\n&lt;/platform&gt;\n</code></pre> <ul> <li><code>src</code>: path to your custom <code>BackgroundFetchHeadlessTask.java</code>.</li> <li><code>target</code>: must be exactly as shown above.</li> </ul>"},{"location":"cordova/BackgroundFetchConfig/#testing-your-headless-task","title":"Testing Your Headless Task","text":"<p>After terminating your app, simulate a fetch event while observing <code>$ adb logcat</code>:</p> <pre><code>adb shell cmd jobscheduler run -f com.your.app.package.name 999\n</code></pre>"},{"location":"cordova/BackgroundFetchConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>forceAlarmManager?: boolean;\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"cordova/BackgroundFetchConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>requiredNetworkType?: NetworkType;\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n});\n</code></pre>"},{"location":"cordova/BackgroundFetchConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>requiresBatteryNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"cordova/BackgroundFetchConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>requiresCharging?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"cordova/BackgroundFetchConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>requiresDeviceIdle?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"cordova/BackgroundFetchConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>requiresStorageNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"cordova/BackgroundFetchConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"cordova/BackgroundFetchConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"cordova/BackgroundFetchStatus/","title":"BackgroundFetchStatus","text":"<p>Authorization status returned by BackgroundFetch.status and BackgroundFetch.configure.</p>"},{"location":"cordova/BackgroundFetchStatus/#status_available","title":"STATUS_AVAILABLE","text":"<pre><code>static STATUS_AVAILABLE: BackgroundFetchStatus = 2;\n</code></pre> <p>Background fetch is available and enabled.</p>"},{"location":"cordova/BackgroundFetchStatus/#status_denied","title":"STATUS_DENIED","text":"<pre><code>static STATUS_DENIED: BackgroundFetchStatus = 1;\n</code></pre> <p>The user explicitly disabled background behavior for this app or for the whole system.</p>"},{"location":"cordova/BackgroundFetchStatus/#status_restricted","title":"STATUS_RESTRICTED","text":"<pre><code>static STATUS_RESTRICTED: BackgroundFetchStatus = 0;\n</code></pre> <p>Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls are in effect).</p>"},{"location":"cordova/HeadlessEvent/","title":"HeadlessEvent","text":"<p>The event object delivered to a headless task handler when the app is running in a terminated (headless) state on Android.</p> <p>Warning</p> <p>You must be prepared to write Java code.</p> <p>Step 1: Create a file named <code>BackgroundFetchHeadlessTask.java</code> in your app (e.g. <code>src/android/</code>). The file can be located anywhere but MUST be named <code>BackgroundFetchHeadlessTask.java</code>:</p> <pre><code>package com.transistorsoft.cordova.backgroundfetch;\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask implements HeadlessTask {\n    @Override\n    public void onFetch(Context context, BGTask task) {\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            Log.d(BackgroundFetch.TAG, \"TIMEOUT: \" + taskId);\n            BackgroundFetch.getInstance(context).finish(taskId);\n            return;\n        }\n        Log.d(BackgroundFetch.TAG, \"onFetch: \" + taskId);\n        // Perform your work here...\n\n        BackgroundFetch.getInstance(context).finish(taskId);\n    }\n}\n</code></pre> <p>Step 2: In your <code>config.xml</code>, add a <code>&lt;resource-file&gt;</code> within <code>&lt;platform name=\"android\"&gt;</code>:</p> <pre><code>&lt;platform name=\"android\"&gt;\n    &lt;resource-file\n        src=\"src/android/BackgroundFetchHeadlessTask.java\"\n        target=\"app/src/main/java/com/transistorsoft/cordova/backgroundfetch/BackgroundFetchHeadlessTask.java\" /&gt;\n&lt;/platform&gt;\n</code></pre> <ul> <li><code>src</code>: path to your custom <code>BackgroundFetchHeadlessTask.java</code>.</li> <li><code>target</code>: must be exactly as shown above.</li> </ul>"},{"location":"cordova/HeadlessEvent/#taskid","title":"taskId","text":"<pre><code>taskId: string;\n</code></pre> <p>The identifier of the task that fired this headless event.</p> <p>Pass this value to BackgroundFetch.finish when your work is complete.</p>"},{"location":"cordova/HeadlessEvent/#timeout","title":"timeout","text":"<pre><code>timeout: boolean;\n</code></pre> <p><code>true</code> when the OS signals that background running-time is nearly exhausted.</p> <p>When a timeout event fires you must immediately call BackgroundFetch.finish and stop any in-progress work.  Failure to do so may cause the OS to penalize your app.</p>"},{"location":"cordova/NetworkType/","title":"NetworkType","text":"<p>Android only Network connectivity constraint for use with requiredNetworkType.</p>"},{"location":"cordova/NetworkType/#network_type_any","title":"NETWORK_TYPE_ANY","text":"<pre><code>static NETWORK_TYPE_ANY: NetworkType = 1;\n</code></pre> <p>Requires any active network connection.</p>"},{"location":"cordova/NetworkType/#network_type_cellular","title":"NETWORK_TYPE_CELLULAR","text":"<pre><code>static NETWORK_TYPE_CELLULAR: NetworkType = 2;\n</code></pre> <p>Requires a cellular (mobile data) network connection.</p>"},{"location":"cordova/NetworkType/#network_type_none","title":"NETWORK_TYPE_NONE","text":"<pre><code>static NETWORK_TYPE_NONE: NetworkType = 0;\n</code></pre> <p>No network constraint \u2014 the task will run regardless of connectivity.  This is the default.</p>"},{"location":"cordova/NetworkType/#network_type_not_roaming","title":"NETWORK_TYPE_NOT_ROAMING","text":"<pre><code>static NETWORK_TYPE_NOT_ROAMING: NetworkType = 4;\n</code></pre> <p>Requires a non-roaming network connection.</p>"},{"location":"cordova/NetworkType/#network_type_unmetered","title":"NETWORK_TYPE_UNMETERED","text":"<pre><code>static NETWORK_TYPE_UNMETERED: NetworkType = 3;\n</code></pre> <p>Requires an unmetered network connection (e.g. Wi-Fi).</p>"},{"location":"cordova/TaskConfig/","title":"TaskConfig","text":"<p>Configuration object passed to BackgroundFetch.scheduleTask.</p>"},{"location":"cordova/TaskConfig/#delay","title":"delay","text":"<pre><code>delay: number;\n</code></pre> <p>The minimum delay in milliseconds before this task fires.</p> <p>The OS may delay the task beyond this value depending on device conditions, battery state, and other scheduling constraints. Example</p> <pre><code>// Fire at least 30 seconds from now.\nBackgroundFetch.scheduleTask({\n  taskId: 'com.foo.task',\n  delay: 30000,\n});\n</code></pre>"},{"location":"cordova/TaskConfig/#periodic","title":"periodic","text":"<pre><code>periodic?: boolean;\n</code></pre> <p>Set <code>true</code> to schedule a repeating task.  Defaults to <code>false</code> (one-shot).</p> <p>When <code>true</code>, the task automatically re-schedules itself after each execution using the same TaskConfig.delay as the repeat interval. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.periodic',\n  delay: 900000,    // 15 minutes in ms\n  periodic: true,\n});\n</code></pre>"},{"location":"cordova/TaskConfig/#requiresnetworkconnectivity","title":"requiresNetworkConnectivity iOS only","text":"<pre><code>requiresNetworkConnectivity?: boolean;\n</code></pre> <p>Set <code>true</code> to require a network connection before this task runs.</p> <p>On Android, use requiredNetworkType instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.networktask',\n  delay: 60000,\n  requiresNetworkConnectivity: true,      // &lt;-- iOS\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY, // &lt;-- Android\n});\n</code></pre>"},{"location":"cordova/TaskConfig/#taskid","title":"taskId","text":"<pre><code>taskId: string;\n</code></pre> <p>A unique identifier for this task.</p> <p>The same <code>taskId</code> is delivered to the <code>onEvent</code> callback registered in BackgroundFetch.configure.  Pass it to BackgroundFetch.finish to signal completion and to BackgroundFetch.stop to cancel the task.</p> <p>Use reverse-domain notation to avoid collisions with other tasks. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.myapp.sync',\n  delay: 60000,\n});\n\n// In your configure() callback:\nBackgroundFetch.configure({}, async (taskId) =&gt; {\n  if (taskId === 'com.myapp.sync') {\n    await performSync();\n  }\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"cordova/TaskConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"cordova/TaskConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Set <code>true</code> to enable the headless mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p> <p>Warning</p> <p>You must be prepared to write Java code.</p>"},{"location":"cordova/TaskConfig/#headless-setup","title":"Headless Setup","text":"<p>Step 1: Create a file named <code>BackgroundFetchHeadlessTask.java</code> in your app (e.g. <code>src/android/</code>). The file can be located anywhere but MUST be named <code>BackgroundFetchHeadlessTask.java</code>:</p> <pre><code>package com.transistorsoft.cordova.backgroundfetch;\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask implements HeadlessTask {\n    @Override\n    public void onFetch(Context context, BGTask task) {\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            Log.d(BackgroundFetch.TAG, \"TIMEOUT: \" + taskId);\n            BackgroundFetch.getInstance(context).finish(taskId);\n            return;\n        }\n        Log.d(BackgroundFetch.TAG, \"onFetch: \" + taskId);\n        // Perform your work here...\n\n        BackgroundFetch.getInstance(context).finish(taskId);\n    }\n}\n</code></pre> <p>Step 2: In your <code>config.xml</code>, add a <code>&lt;resource-file&gt;</code> within <code>&lt;platform name=\"android\"&gt;</code>:</p> <pre><code>&lt;platform name=\"android\"&gt;\n    &lt;resource-file\n        src=\"src/android/BackgroundFetchHeadlessTask.java\"\n        target=\"app/src/main/java/com/transistorsoft/cordova/backgroundfetch/BackgroundFetchHeadlessTask.java\" /&gt;\n&lt;/platform&gt;\n</code></pre> <ul> <li><code>src</code>: path to your custom <code>BackgroundFetchHeadlessTask.java</code>.</li> <li><code>target</code>: must be exactly as shown above.</li> </ul>"},{"location":"cordova/TaskConfig/#testing-your-headless-task","title":"Testing Your Headless Task","text":"<p>After terminating your app, simulate a fetch event while observing <code>$ adb logcat</code>:</p> <pre><code>adb shell cmd jobscheduler run -f com.your.app.package.name 999\n</code></pre>"},{"location":"cordova/TaskConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>forceAlarmManager?: boolean;\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"cordova/TaskConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>requiredNetworkType?: NetworkType;\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n});\n</code></pre>"},{"location":"cordova/TaskConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>requiresBatteryNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"cordova/TaskConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>requiresCharging?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"cordova/TaskConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>requiresDeviceIdle?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"cordova/TaskConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>requiresStorageNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"cordova/TaskConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"cordova/TaskConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"cordova/debugging/","title":"Debugging","text":"<p>Background Fetch events are notoriously difficult to test because the OS controls when they fire.  Use these simulation techniques during development.</p>"},{"location":"cordova/debugging/#ios","title":"iOS","text":""},{"location":"cordova/debugging/#simulating-fetch-events","title":"Simulating Fetch Events","text":"<p>iOS 13+ uses the <code>BGTaskScheduler</code> API.  To simulate a fetch event:</p> <ol> <li>Run your app in Xcode.</li> <li>Click the Pause button () to initiate a breakpoint.</li> <li>In the <code>(lldb)</code> console, paste:</li> </ol> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <ol> <li>Click the Continue button ().  The callback provided to <code>configure</code> will receive the event.</li> </ol> <p></p> <p></p> <p></p>"},{"location":"cordova/debugging/#simulating-custom-scheduletask-events","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<p>If you've registered a custom task (e.g. <code>com.transistorsoft.customtask</code>), simulate it with:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.customtask\"]\n</code></pre>"},{"location":"cordova/debugging/#simulating-task-timeout-events","title":"Simulating Task Timeout Events","text":"<p>To simulate a task timeout, do not call <code>finish(taskId)</code> in your event callback, then run:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateExpirationForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <p>Your timeout callback will fire, allowing you to verify your timeout handling logic.</p>"},{"location":"cordova/debugging/#ios-tips","title":"iOS Tips","text":"<p>Warning</p> <p>iOS can take hours or even days before Apple's machine-learning algorithm begins regularly firing fetch events.  Do not sit staring at your logs waiting for an event.  If your simulated events work, everything is correctly configured.</p> <ul> <li>If the user doesn't open your app for long periods of time, iOS will stop firing events.</li> <li><code>scheduleTask</code> on iOS seems only to run when the device is plugged into power.</li> <li>There is no <code>stopOnTerminate: false</code> for iOS \u2014 when the app is terminated, iOS stops firing events.</li> </ul>"},{"location":"cordova/debugging/#android","title":"Android","text":""},{"location":"cordova/debugging/#observing-logs","title":"Observing Logs","text":"<p>Use <code>adb logcat</code> to observe plugin logs:</p> <pre><code>adb logcat \"*:S\" TSBackgroundFetch:V\n</code></pre>"},{"location":"cordova/debugging/#simulating-fetch-events_1","title":"Simulating Fetch Events","text":"<p>Simulate a background-fetch event (replace <code>&lt;your.application.id&gt;</code> with your app's package name):</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre>"},{"location":"cordova/debugging/#simulating-custom-scheduletask-events_1","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<ol> <li>Observe <code>adb logcat</code> for the <code>registerTask</code> log entry and copy the <code>jobId</code>:</li> </ol> <pre><code>TSBackgroundFetch: - registerTask: com.your.package.name (jobId: -359368280)\n</code></pre> <ol> <li>Paste the <code>jobId</code> into the <code>adb shell</code> command:</li> </ol> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; -359368280\n</code></pre>"},{"location":"cordova/debugging/#simulating-headless-events","title":"Simulating Headless Events","text":"<p>After terminating your app, simulate a fetch event to test your headless task:</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre> <p>Verify the headless task executes by observing <code>adb logcat</code> output.</p>"},{"location":"cordova/examples/","title":"Examples","text":""},{"location":"cordova/examples/#basic-usage","title":"Basic Usage","text":"<pre><code>import BackgroundFetch from \"cordova-plugin-background-fetch\";\n\nconst status = await BackgroundFetch.configure(\n  {\n    minimumFetchInterval: 15, // minutes\n  },\n  function (taskId) {\n    // &lt;-- Event callback\n    console.log(\"[BackgroundFetch] Event received:\", taskId);\n    // Perform your work here...\n    BackgroundFetch.finish(taskId);\n  },\n  function (taskId) {\n    // &lt;-- Timeout callback\n    console.log(\"[BackgroundFetch] TIMEOUT:\", taskId);\n    BackgroundFetch.finish(taskId);\n  }\n);\nconsole.log(\"[BackgroundFetch] configure status:\", status);\n</code></pre>"},{"location":"cordova/examples/#scheduling-a-custom-task","title":"Scheduling a Custom Task","text":"<pre><code>BackgroundFetch.scheduleTask({\n  taskId: \"com.example.my-custom-task\",\n  delay: 60000, // milliseconds\n  periodic: false,\n  forceAlarmManager: true,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,\n});\n</code></pre> <p>All scheduled tasks fire into the same callback provided to <code>configure</code>.  Use a <code>switch</code> statement to route by <code>taskId</code>:</p> <pre><code>BackgroundFetch.configure(\n  { minimumFetchInterval: 15 },\n  function (taskId) {\n    switch (taskId) {\n      case \"com.example.my-custom-task\":\n        console.log(\"Custom task fired\");\n        break;\n      default:\n        console.log(\"Default fetch event\");\n    }\n    BackgroundFetch.finish(taskId);\n  },\n  function (taskId) {\n    BackgroundFetch.finish(taskId);\n  }\n);\n</code></pre>"},{"location":"cordova/examples/#headless-task-android","title":"Headless Task (Android)","text":"<p>When the app is terminated, Android can continue executing background-fetch events via a custom Java class.</p> <p>Warning</p> <p>You must be prepared to write Java code.</p> <p>Step 1: Create <code>BackgroundFetchHeadlessTask.java</code> (e.g. in <code>src/android/</code>):</p> <pre><code>package com.transistorsoft.cordova.backgroundfetch;\n\nimport android.content.Context;\nimport android.util.Log;\n\nimport com.transistorsoft.tsbackgroundfetch.BackgroundFetch;\nimport com.transistorsoft.tsbackgroundfetch.BGTask;\n\npublic class BackgroundFetchHeadlessTask implements HeadlessTask {\n    @Override\n    public void onFetch(Context context, BGTask task) {\n        String taskId = task.getTaskId();\n        boolean isTimeout = task.getTimedOut();\n        if (isTimeout) {\n            Log.d(BackgroundFetch.TAG, \"TIMEOUT: \" + taskId);\n            BackgroundFetch.getInstance(context).finish(taskId);\n            return;\n        }\n        Log.d(BackgroundFetch.TAG, \"onFetch: \" + taskId);\n        // Perform your work here...\n\n        BackgroundFetch.getInstance(context).finish(taskId);\n    }\n}\n</code></pre> <p>Step 2: In your <code>config.xml</code>, add a <code>&lt;resource-file&gt;</code> within <code>&lt;platform name=\"android\"&gt;</code>:</p> <pre><code>&lt;platform name=\"android\"&gt;\n    &lt;resource-file\n        src=\"src/android/BackgroundFetchHeadlessTask.java\"\n        target=\"app/src/main/java/com/transistorsoft/cordova/backgroundfetch/BackgroundFetchHeadlessTask.java\" /&gt;\n&lt;/platform&gt;\n</code></pre> <p>Note</p> <p>See enableHeadless for full setup details. Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"cordova/examples/#checking-status","title":"Checking Status","text":"<pre><code>var status = await BackgroundFetch.status;\n\nswitch (status) {\n  case BackgroundFetch.STATUS_AVAILABLE:\n    console.log(\"Background fetch is available\");\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    console.log(\"Background fetch is denied by the user\");\n    break;\n  case BackgroundFetch.STATUS_RESTRICTED:\n    console.log(\"Background fetch is restricted (e.g. parental controls)\");\n    break;\n}\n</code></pre>"},{"location":"cordova/examples/#stopping-tasks","title":"Stopping Tasks","text":"<pre><code>// Stop a specific scheduled task\nBackgroundFetch.stop(\"com.example.my-custom-task\");\n\n// Stop ALL tasks (including the default fetch event)\nBackgroundFetch.stop();\n</code></pre>"},{"location":"cordova/setup/","title":"Cordova \u2014 Setup","text":""},{"location":"cordova/setup/#installation","title":"Installation","text":"IonicCordova <pre><code>ionic cordova plugin add cordova-plugin-background-fetch\n</code></pre> <pre><code>cordova plugin add cordova-plugin-background-fetch\n</code></pre>"},{"location":"cordova/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"cordova/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your project root \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Background fetch</li> <li>[x] Background processing (only if you intend to use <code>BackgroundFetch.scheduleTask</code>)</li> </ul> <p></p>"},{"location":"cordova/setup/#infoplist","title":"Info.plist","text":"<p>If you intend to execute custom tasks via <code>BackgroundFetch.scheduleTask</code>, you must register their identifiers in your <code>config.xml</code> within the <code>&lt;platform name=\"ios\"&gt;</code> block:</p> <pre><code>&lt;platform name=\"ios\"&gt;\n    &lt;config-file parent=\"BGTaskSchedulerPermittedIdentifiers\"\n                 target=\"*-Info.plist\"&gt;\n        &lt;array&gt;\n            &lt;string&gt;com.transistorsoft.customtask&lt;/string&gt;\n        &lt;/array&gt;\n    &lt;/config-file&gt;\n&lt;/platform&gt;\n</code></pre> <p>Warning</p> <p>A task identifier can be any string you wish, but it must be prefixed with <code>com.transistorsoft.</code>.</p> <p>Note</p> <p>The base identifier <code>com.transistorsoft.fetch</code> is registered automatically by the plugin \u2014 you do not need to add it to <code>config.xml</code>.</p>"},{"location":"flutter/","title":"Flutter \u2014 Background Fetch","text":"<p>Cross-platform background fetch API for Flutter on iOS &amp; Android.</p> <ul> <li> GitHub Repo</li> <li>Setup Guide</li> <li>API Reference \u2014 BackgroundFetch</li> <li>API Reference \u2014 BackgroundFetchConfig</li> <li>API Reference \u2014 TaskConfig</li> </ul>"},{"location":"flutter/BackgroundFetch/","title":"BackgroundFetch","text":""},{"location":"flutter/BackgroundFetch/#configure","title":"configure","text":"<pre><code>static Future&lt;int&gt; configure(BackgroundFetchConfig config, Function(String) onFetch, [Function(String)? onTimeout])\n</code></pre> <p>Configure the plugin and begin listening for background-fetch events.</p> <p>Calling <code>configure</code> automatically starts background-fetch (equivalent to calling BackgroundFetch.start immediately after configuration).</p> <p>The BackgroundFetchConfig.minimumFetchInterval option controls how frequently iOS will wake your app in the background.  Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports additional scheduling constraints such as requiredNetworkType, requiresCharging, etc.</p> <p>Warning</p> <p>You must call BackgroundFetch.finish within your event callback.  Failure to do so will result in the OS throttling future background events for your app.</p> <p>Basic configuration</p> <p><pre><code>int status = await BackgroundFetch.configure(BackgroundFetchConfig(\n  minimumFetchInterval: 15,\n), (String taskId) async {\n  print('[BackgroundFetch] taskId: $taskId');\n  BackgroundFetch.finish(taskId);\n}, (String taskId) {\n  print('[BackgroundFetch] TIMEOUT: $taskId');\n  BackgroundFetch.finish(taskId);\n});\n\nprint('[BackgroundFetch] status: $status');\n</code></pre> Android configuration</p> <pre><code>BackgroundFetch.configure(BackgroundFetchConfig(\n  minimumFetchInterval: 15,\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n  requiresBatteryNotLow: true,\n), (String taskId) async {\n  print('[BackgroundFetch] taskId: $taskId');\n  BackgroundFetch.finish(taskId);\n}, (String taskId) {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"flutter/BackgroundFetch/#finish","title":"finish","text":"<pre><code>static Future&lt;void&gt; finish(String taskId)\n</code></pre> <p>Signal to the OS that your background task is complete.</p> <p>Warning</p> <p>You must call <code>finish</code> at the end of every background-fetch event callback. Failure to call <code>finish</code> has serious consequences:</p> <ul> <li>iOS \u2014 The OS may terminate your app and penalise future background-wake frequency.</li> <li>Android \u2014 The <code>JobScheduler</code> job will not be considered complete and the OS may   throttle or block future executions.</li> </ul> <p>Always call <code>finish</code> as quickly as possible, even when your task encounters an error. For timeout events, call <code>finish</code> immediately without doing any additional work. Example</p> <pre><code>BackgroundFetch.configure(BackgroundFetchConfig(), (String taskId) async {\n  try {\n    await doMyWork();\n  } catch (e) {\n    print('Error: $e');\n  } finally {\n    BackgroundFetch.finish(taskId);\n  }\n}, (String taskId) {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"flutter/BackgroundFetch/#registerheadlesstask","title":"registerHeadlessTask Android only","text":"<pre><code>static Future&lt;bool&gt; registerHeadlessTask(Function(HeadlessEvent) callback)\n</code></pre> <p>Register a function to execute when the app is terminated while background-fetch events remain active.</p> <p>Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"flutter/BackgroundFetch/#how-it-works","title":"How it works","text":"<p>When Android terminates the app, the native module launches a minimal JS context (without the React Native UI) and invokes your headless task function.  This allows you to handle background events even when the app is not running.</p>"},{"location":"flutter/BackgroundFetch/#registration","title":"Registration","text":"<p>Call <code>registerHeadlessTask</code> at your app's entry point (<code>index.js</code>), outside any React component.  The registration must happen before the OS attempts to fire a headless event.</p>"},{"location":"flutter/BackgroundFetch/#restrictions","title":"Restrictions","text":"<ul> <li>Do not import modules that depend on the React Native UI runtime (e.g. components,   navigation, Redux stores that render UI).</li> <li>Keep the task lightweight \u2014 the OS may kill the JS context at any time.</li> <li>Always call BackgroundFetch.finish before the returned <code>Promise</code> resolves. Example</li> </ul> <pre><code>// main.dart \u2014 register at the top level, annotated with @pragma.\n@pragma('vm:entry-point')\nvoid backgroundFetchHeadlessTask(HeadlessEvent task) async {\n  String taskId = task.taskId;\n  bool isTimeout = task.timeout;\n\n  if (isTimeout) {\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  print('[BackgroundFetch] Headless event, taskId: $taskId');\n  // ... perform lightweight work ...\n  BackgroundFetch.finish(taskId);\n}\n\nvoid main() {\n  BackgroundFetch.registerHeadlessTask(backgroundFetchHeadlessTask);\n  runApp(MyApp());\n}\n</code></pre>"},{"location":"flutter/BackgroundFetch/#scheduletask","title":"scheduleTask","text":"<pre><code>static Future&lt;bool&gt; scheduleTask(TaskConfig config)\n</code></pre> <p>Schedule a custom one-shot or periodic background task in addition to the default fetch event registered with BackgroundFetch.configure.</p> <p>Custom tasks fire the same <code>onEvent</code> callback registered in BackgroundFetch.configure, identified by their TaskConfig.taskId.  Call BackgroundFetch.finish with that <code>taskId</code> to signal completion, and BackgroundFetch.stop with the <code>taskId</code> to cancel.</p>"},{"location":"flutter/BackgroundFetch/#ios","title":"iOS","text":"<p>On iOS, custom tasks are registered using the <code>BGProcessingTask</code> API.  You must declare each <code>taskId</code> in your app's <code>Info.plist</code> under <code>BGTaskSchedulerPermittedIdentifiers</code> before calling <code>scheduleTask</code>.</p>"},{"location":"flutter/BackgroundFetch/#android","title":"Android","text":"<p>Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports all AbstractConfig scheduling constraints. One-shot task</p> <p><pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.customtask',\n  delay: 5000,\n  stopOnTerminate: false,\n));\n</code></pre> Periodic task</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.periodic-sync',\n  delay: 600000,\n  periodic: true,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,\n  requiresNetworkConnectivity: true,\n));\n</code></pre>"},{"location":"flutter/BackgroundFetch/#start","title":"start","text":"<pre><code>static Future&lt;int&gt; start()\n</code></pre> <p>Start subscribing to background-fetch events.</p> <p>BackgroundFetch.configure calls <code>start</code> automatically.  Use <code>start</code> explicitly only after a previous call to BackgroundFetch.stop. Example</p> <pre><code>int status = await BackgroundFetch.start();\nprint('[BackgroundFetch] started, status: $status');\n</code></pre>"},{"location":"flutter/BackgroundFetch/#status","title":"status","text":"<pre><code>static Future&lt;int&gt; get status\n</code></pre> <p>Query the current authorization status of the Background Fetch API.</p> Value Constant Description <code>0</code> BackgroundFetchStatus.STATUS_RESTRICTED Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls). <code>1</code> BackgroundFetchStatus.STATUS_DENIED The user explicitly disabled background behavior for this app or the whole system. <code>2</code> BackgroundFetchStatus.STATUS_AVAILABLE Background fetch is available and enabled. Example <pre><code>int status = await BackgroundFetch.status;\nswitch (status) {\n  case BackgroundFetch.STATUS_RESTRICTED:\n    print('Background fetch restricted');\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    print('Background fetch denied by user');\n    break;\n  case BackgroundFetch.STATUS_AVAILABLE:\n    print('Background fetch available');\n    break;\n}\n</code></pre>"},{"location":"flutter/BackgroundFetch/#stop","title":"stop","text":"<pre><code>static Future&lt;int&gt; stop([String? taskId])\n</code></pre> <p>Stop subscribing to background-fetch events.</p> <ul> <li>If a <code>taskId</code> is provided, only that specific scheduled task is cancelled.</li> <li>If no <code>taskId</code> is provided, all background-fetch and scheduled tasks are cancelled.</li> </ul> <p>Use BackgroundFetch.start to resume after stopping. Example</p> <pre><code>// Stop all background-fetch events.\nawait BackgroundFetch.stop();\n\n// Stop a single scheduled task by taskId.\nawait BackgroundFetch.stop('com.foo.customtask');\n</code></pre>"},{"location":"flutter/BackgroundFetchConfig/","title":"BackgroundFetchConfig","text":"<p>Configuration object passed to BackgroundFetch.configure.</p>"},{"location":"flutter/BackgroundFetchConfig/#minimumfetchinterval","title":"minimumFetchInterval","text":"<pre><code>int? minimumFetchInterval\n</code></pre> <p>The minimum interval in minutes between background-fetch events. Defaults to <code>15</code> minutes.  The minimum allowed value is <code>15</code> minutes.</p> <p>Note</p> <p>The OS does not guarantee fetch events will fire at exactly this interval. iOS adjusts timing based on usage patterns and battery state.  This is a minimum hint, not a precise schedule.</p> <p>Example</p> <pre><code>BackgroundFetch.configure(BackgroundFetchConfig(\n  minimumFetchInterval: 15,\n), (String taskId) async {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"flutter/BackgroundFetchConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"flutter/BackgroundFetchConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>bool? enableHeadless\n</code></pre> <p>Set <code>true</code> to enable the headless mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p> <p>\ud83d\udcc2 <code>lib/main.dart</code>:</p> <pre><code>import 'package:flutter/material.dart';\nimport 'package:background_fetch/background_fetch.dart';\n\n// This \"Headless Task\" is run when the app is terminated.\n// Must be a top-level or static function.\n@pragma('vm:entry-point')\nvoid backgroundFetchHeadlessTask(HeadlessEvent task) async {\n  String taskId = task.taskId;\n  bool isTimeout = task.timeout;\n  if (isTimeout) {\n    // This task has exceeded its allowed running-time.\n    print(\"[BackgroundFetch] Headless TIMEOUT: $taskId\");\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  print(\"[BackgroundFetch] Headless event received: $taskId\");\n  BackgroundFetch.finish(taskId);\n}\n\nvoid main() {\n  runApp(MyApp());\n\n  // Register to receive BackgroundFetch events after app is terminated.\n  // Requires {stopOnTerminate: false, enableHeadless: true}\n  BackgroundFetch.registerHeadlessTask(backgroundFetchHeadlessTask);\n}\n</code></pre>"},{"location":"flutter/BackgroundFetchConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>bool? forceAlarmManager\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"flutter/BackgroundFetchConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>int? requiredNetworkType\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n));\n</code></pre>"},{"location":"flutter/BackgroundFetchConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>bool? requiresBatteryNotLow\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"flutter/BackgroundFetchConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>bool? requiresCharging\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"flutter/BackgroundFetchConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>bool? requiresDeviceIdle\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"flutter/BackgroundFetchConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>bool? requiresStorageNotLow\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"flutter/BackgroundFetchConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>bool? startOnBoot\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"flutter/BackgroundFetchConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>bool? stopOnTerminate\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure(BackgroundFetchConfig(\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n), (String taskId) async {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"flutter/BackgroundFetchStatus/","title":"BackgroundFetchStatus","text":"<p>Authorization status returned by BackgroundFetch.status and BackgroundFetch.configure.</p>"},{"location":"flutter/BackgroundFetchStatus/#status_available","title":"STATUS_AVAILABLE","text":"<pre><code>static const int STATUS_AVAILABLE = 2;\n</code></pre> <p>Background fetch is available and enabled.</p>"},{"location":"flutter/BackgroundFetchStatus/#status_denied","title":"STATUS_DENIED","text":"<pre><code>static const int STATUS_DENIED = 1;\n</code></pre> <p>The user explicitly disabled background behavior for this app or for the whole system.</p>"},{"location":"flutter/BackgroundFetchStatus/#status_restricted","title":"STATUS_RESTRICTED","text":"<pre><code>static const int STATUS_RESTRICTED = 0;\n</code></pre> <p>Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls are in effect).</p>"},{"location":"flutter/HeadlessEvent/","title":"HeadlessEvent","text":"<p>The event object delivered to a BackgroundFetch.registerHeadlessTask handler when the app is running in a terminated (headless) state on Android.</p> Property Type Description taskId <code>String</code> Identifier of the task that fired. Pass to BackgroundFetch.finish. timeout <code>bool</code> <code>true</code> when the OS signals that background time is nearly exhausted. <p>\ud83d\udcc2 <code>lib/main.dart</code>:</p> <pre><code>import 'package:flutter/material.dart';\nimport 'package:background_fetch/background_fetch.dart';\n\n// This \"Headless Task\" is run when the app is terminated.\n// Must be a top-level or static function.\n@pragma('vm:entry-point')\nvoid backgroundFetchHeadlessTask(HeadlessEvent task) async {\n  String taskId = task.taskId;\n  bool isTimeout = task.timeout;\n  if (isTimeout) {\n    // This task has exceeded its allowed running-time.\n    print(\"[BackgroundFetch] Headless TIMEOUT: $taskId\");\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  print(\"[BackgroundFetch] Headless event received: $taskId\");\n  BackgroundFetch.finish(taskId);\n}\n\nvoid main() {\n  runApp(MyApp());\n\n  // Register to receive BackgroundFetch events after app is terminated.\n  // Requires {stopOnTerminate: false, enableHeadless: true}\n  BackgroundFetch.registerHeadlessTask(backgroundFetchHeadlessTask);\n}\n</code></pre>"},{"location":"flutter/HeadlessEvent/#taskid","title":"taskId","text":"<pre><code>String taskId\n</code></pre> <p>The identifier of the task that fired this headless event.</p> <p>Pass this value to BackgroundFetch.finish when your work is complete.</p>"},{"location":"flutter/HeadlessEvent/#timeout","title":"timeout","text":"<pre><code>bool timeout\n</code></pre> <p><code>true</code> when the OS signals that background running-time is nearly exhausted.</p> <p>When a timeout event fires you must immediately call BackgroundFetch.finish and stop any in-progress work.  Failure to do so may cause the OS to penalize your app.</p>"},{"location":"flutter/NetworkType/","title":"NetworkType","text":"<p>Android only Network connectivity constraint for use with requiredNetworkType.</p>"},{"location":"flutter/NetworkType/#network_type_any","title":"NETWORK_TYPE_ANY","text":"<pre><code>NetworkType.ANY\n</code></pre> <p>Requires any active network connection.</p>"},{"location":"flutter/NetworkType/#network_type_cellular","title":"NETWORK_TYPE_CELLULAR","text":"<pre><code>NetworkType.CELLULAR\n</code></pre> <p>Requires a cellular (mobile data) network connection.</p>"},{"location":"flutter/NetworkType/#network_type_none","title":"NETWORK_TYPE_NONE","text":"<pre><code>NetworkType.NONE\n</code></pre> <p>No network constraint \u2014 the task will run regardless of connectivity.  This is the default.</p>"},{"location":"flutter/NetworkType/#network_type_not_roaming","title":"NETWORK_TYPE_NOT_ROAMING","text":"<pre><code>NetworkType.NOT_ROAMING\n</code></pre> <p>Requires a non-roaming network connection.</p>"},{"location":"flutter/NetworkType/#network_type_unmetered","title":"NETWORK_TYPE_UNMETERED","text":"<pre><code>NetworkType.UNMETERED\n</code></pre> <p>Requires an unmetered network connection (e.g. Wi-Fi).</p>"},{"location":"flutter/TaskConfig/","title":"TaskConfig","text":"<p>Configuration object passed to BackgroundFetch.scheduleTask.</p>"},{"location":"flutter/TaskConfig/#delay","title":"delay","text":"<pre><code>int delay\n</code></pre> <p>The minimum delay in milliseconds before this task fires.</p> <p>The OS may delay the task beyond this value depending on device conditions, battery state, and other scheduling constraints. Example</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.task',\n  delay: 30000,\n));\n</code></pre>"},{"location":"flutter/TaskConfig/#periodic","title":"periodic","text":"<pre><code>bool? periodic\n</code></pre> <p>Set <code>true</code> to schedule a repeating task.  Defaults to <code>false</code> (one-shot).</p> <p>When <code>true</code>, the task automatically re-schedules itself after each execution using the same TaskConfig.delay as the repeat interval. Example</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.periodic',\n  delay: 900000,\n  periodic: true,\n));\n</code></pre>"},{"location":"flutter/TaskConfig/#requiresnetworkconnectivity","title":"requiresNetworkConnectivity iOS only","text":"<pre><code>bool? requiresNetworkConnectivity\n</code></pre> <p>Set <code>true</code> to require a network connection before this task runs.</p> <p>On Android, use requiredNetworkType instead. Example</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.networktask',\n  delay: 60000,\n  requiresNetworkConnectivity: true,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,\n));\n</code></pre>"},{"location":"flutter/TaskConfig/#taskid","title":"taskId","text":"<pre><code>String taskId\n</code></pre> <p>A unique identifier for this task.</p> <p>The same <code>taskId</code> is delivered to the <code>onEvent</code> callback registered in BackgroundFetch.configure.  Pass it to BackgroundFetch.finish to signal completion and to BackgroundFetch.stop to cancel the task.</p> <p>Use reverse-domain notation to avoid collisions with other tasks. Example</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.myapp.sync',\n  delay: 60000,\n));\n</code></pre>"},{"location":"flutter/TaskConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"flutter/TaskConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>bool? enableHeadless\n</code></pre> <p>Set <code>true</code> to enable the headless mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p> <p>\ud83d\udcc2 <code>lib/main.dart</code>:</p> <pre><code>import 'package:flutter/material.dart';\nimport 'package:background_fetch/background_fetch.dart';\n\n// This \"Headless Task\" is run when the app is terminated.\n// Must be a top-level or static function.\n@pragma('vm:entry-point')\nvoid backgroundFetchHeadlessTask(HeadlessEvent task) async {\n  String taskId = task.taskId;\n  bool isTimeout = task.timeout;\n  if (isTimeout) {\n    // This task has exceeded its allowed running-time.\n    print(\"[BackgroundFetch] Headless TIMEOUT: $taskId\");\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  print(\"[BackgroundFetch] Headless event received: $taskId\");\n  BackgroundFetch.finish(taskId);\n}\n\nvoid main() {\n  runApp(MyApp());\n\n  // Register to receive BackgroundFetch events after app is terminated.\n  // Requires {stopOnTerminate: false, enableHeadless: true}\n  BackgroundFetch.registerHeadlessTask(backgroundFetchHeadlessTask);\n}\n</code></pre>"},{"location":"flutter/TaskConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>bool? forceAlarmManager\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"flutter/TaskConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>int? requiredNetworkType\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n));\n</code></pre>"},{"location":"flutter/TaskConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>bool? requiresBatteryNotLow\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"flutter/TaskConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>bool? requiresCharging\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"flutter/TaskConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>bool? requiresDeviceIdle\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"flutter/TaskConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>bool? requiresStorageNotLow\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"flutter/TaskConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>bool? startOnBoot\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"flutter/TaskConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>bool? stopOnTerminate\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure(BackgroundFetchConfig(\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n), (String taskId) async {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"flutter/debugging/","title":"Debugging","text":"<p>Background Fetch events are notoriously difficult to test because the OS controls when they fire.  Use these simulation techniques during development.</p>"},{"location":"flutter/debugging/#ios","title":"iOS","text":""},{"location":"flutter/debugging/#simulating-fetch-events","title":"Simulating Fetch Events","text":"<p>iOS 13+ uses the <code>BGTaskScheduler</code> API.  To simulate a fetch event:</p> <ol> <li>Run your app in Xcode.</li> <li>Click the Pause button () to initiate a breakpoint.</li> <li>In the <code>(lldb)</code> console, paste:</li> </ol> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <ol> <li>Click the Continue button ().  The callback provided to <code>configure</code> will receive the event.</li> </ol> <p></p> <p></p> <p></p>"},{"location":"flutter/debugging/#simulating-custom-scheduletask-events","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<p>If you've registered a custom task (e.g. <code>com.transistorsoft.customtask</code>), simulate it with:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.customtask\"]\n</code></pre>"},{"location":"flutter/debugging/#simulating-task-timeout-events","title":"Simulating Task Timeout Events","text":"<p>To simulate a task timeout, do not call <code>finish(taskId)</code> in your event callback, then run:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateExpirationForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <p>Your timeout callback will fire, allowing you to verify your timeout handling logic.</p>"},{"location":"flutter/debugging/#ios-tips","title":"iOS Tips","text":"<p>Warning</p> <p>iOS can take hours or even days before Apple's machine-learning algorithm begins regularly firing fetch events.  Do not sit staring at your logs waiting for an event.  If your simulated events work, everything is correctly configured.</p> <ul> <li>If the user doesn't open your app for long periods of time, iOS will stop firing events.</li> <li><code>scheduleTask</code> on iOS seems only to run when the device is plugged into power.</li> <li>There is no <code>stopOnTerminate: false</code> for iOS \u2014 when the app is terminated, iOS stops firing events.</li> </ul>"},{"location":"flutter/debugging/#android","title":"Android","text":""},{"location":"flutter/debugging/#observing-logs","title":"Observing Logs","text":"<p>Use <code>adb logcat</code> to observe plugin logs:</p> <pre><code>adb logcat \"*:S\" TSBackgroundFetch:V\n</code></pre>"},{"location":"flutter/debugging/#simulating-fetch-events_1","title":"Simulating Fetch Events","text":"<p>Simulate a background-fetch event (replace <code>&lt;your.application.id&gt;</code> with your app's package name):</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre>"},{"location":"flutter/debugging/#simulating-custom-scheduletask-events_1","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<ol> <li>Observe <code>adb logcat</code> for the <code>registerTask</code> log entry and copy the <code>jobId</code>:</li> </ol> <pre><code>TSBackgroundFetch: - registerTask: com.your.package.name (jobId: -359368280)\n</code></pre> <ol> <li>Paste the <code>jobId</code> into the <code>adb shell</code> command:</li> </ol> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; -359368280\n</code></pre>"},{"location":"flutter/debugging/#simulating-headless-events","title":"Simulating Headless Events","text":"<p>After terminating your app, simulate a fetch event to test your headless task:</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre> <p>Verify the headless task executes by observing <code>adb logcat</code> output.</p>"},{"location":"flutter/examples/","title":"Examples","text":""},{"location":"flutter/examples/#basic-usage","title":"Basic Usage","text":"<pre><code>import 'package:background_fetch/background_fetch.dart';\n\nclass _MyAppState extends State&lt;MyApp&gt; {\n  @override\n  void initState() {\n    super.initState();\n    initBackgroundFetch();\n  }\n\n  Future&lt;void&gt; initBackgroundFetch() async {\n    int status = await BackgroundFetch.configure(\n      BackgroundFetchConfig(\n        minimumFetchInterval: 15, // minutes\n        stopOnTerminate: false,\n        startOnBoot: true,\n        enableHeadless: true,\n      ),\n      (String taskId) async {\n        // &lt;-- Event callback\n        print(\"[BackgroundFetch] Event received: $taskId\");\n        // Perform your work here...\n        BackgroundFetch.finish(taskId);\n      },\n      (String taskId) async {\n        // &lt;-- Timeout callback\n        print(\"[BackgroundFetch] TIMEOUT: $taskId\");\n        BackgroundFetch.finish(taskId);\n      },\n    );\n    print(\"[BackgroundFetch] configure status: $status\");\n  }\n}\n</code></pre>"},{"location":"flutter/examples/#scheduling-a-custom-task","title":"Scheduling a Custom Task","text":"<pre><code>BackgroundFetch.scheduleTask(TaskConfig(\n  taskId: \"com.example.my-custom-task\",\n  delay: 60000, // milliseconds\n  periodic: false,\n  forceAlarmManager: true,\n  requiredNetworkType: NetworkType.ANY,\n));\n</code></pre> <p>All scheduled tasks fire into the same callback provided to <code>configure</code>.  Use a <code>switch</code> statement to route by <code>taskId</code>:</p> <pre><code>int status = await BackgroundFetch.configure(\n  BackgroundFetchConfig(minimumFetchInterval: 15),\n  (String taskId) async {\n    switch (taskId) {\n      case \"com.example.my-custom-task\":\n        print(\"Custom task fired\");\n        break;\n      default:\n        print(\"Default fetch event\");\n    }\n    BackgroundFetch.finish(taskId);\n  },\n  (String taskId) async {\n    BackgroundFetch.finish(taskId);\n  },\n);\n</code></pre>"},{"location":"flutter/examples/#headless-task-android","title":"Headless Task (Android)","text":"<p>When the app is terminated, Android can continue executing background-fetch events via a headless Dart isolate.</p> <p>Register your headless task in <code>lib/main.dart</code>.  The callback must be a top-level or static function annotated with <code>@pragma('vm:entry-point')</code>:</p> <pre><code>import 'package:flutter/material.dart';\nimport 'package:background_fetch/background_fetch.dart';\n\n@pragma('vm:entry-point')\nvoid backgroundFetchHeadlessTask(HeadlessEvent task) async {\n  String taskId = task.taskId;\n  bool isTimeout = task.timeout;\n\n  if (isTimeout) {\n    print(\"[BackgroundFetch] Headless TIMEOUT: $taskId\");\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n\n  print(\"[BackgroundFetch] Headless event: $taskId\");\n  // Perform your work here...\n  BackgroundFetch.finish(taskId);\n}\n\nvoid main() {\n  runApp(MyApp());\n\n  // Register to receive BackgroundFetch events after app is terminated.\n  BackgroundFetch.registerHeadlessTask(backgroundFetchHeadlessTask);\n}\n</code></pre> <p>Note</p> <p>See enableHeadless for full setup details. Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"flutter/examples/#checking-status","title":"Checking Status","text":"<pre><code>int status = await BackgroundFetch.status;\n\nswitch (status) {\n  case BackgroundFetch.STATUS_AVAILABLE:\n    print(\"Background fetch is available\");\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    print(\"Background fetch is denied by the user\");\n    break;\n  case BackgroundFetch.STATUS_RESTRICTED:\n    print(\"Background fetch is restricted (e.g. parental controls)\");\n    break;\n}\n</code></pre>"},{"location":"flutter/examples/#stopping-tasks","title":"Stopping Tasks","text":"<pre><code>// Stop a specific scheduled task\nawait BackgroundFetch.stop(\"com.example.my-custom-task\");\n\n// Stop ALL tasks (including the default fetch event)\nawait BackgroundFetch.stop();\n</code></pre>"},{"location":"flutter/setup/","title":"Flutter \u2014 Setup","text":""},{"location":"flutter/setup/#installation","title":"Installation","text":"<pre><code>flutter pub add background_fetch\n</code></pre>"},{"location":"flutter/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"flutter/setup/#cocoapods","title":"CocoaPods","text":"<p>If you're not using Swift Package Manager, adjust <code>use_frameworks!</code> in your <code>ios/Podfile</code>:</p> <pre><code>target 'Runner' do\n  use_frameworks! :linkage =&gt; :static   # &lt;-- append :linkage =&gt; :static\nend\n</code></pre> <p>Warning</p> <p>Without <code>:linkage =&gt; :static</code>, you'll get a build error about statically linked binaries in the <code>TSBackgroundFetch.xcframework</code>.</p>"},{"location":"flutter/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your project root \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Background fetch</li> <li>[x] Background processing (only if you intend to use <code>BackgroundFetch.scheduleTask</code>)</li> </ul> <p></p>"},{"location":"flutter/setup/#infoplist","title":"Info.plist","text":"<p>Open your Info.plist and add the key \"Permitted background task scheduler identifiers\":</p> <p></p> <p>Add the required identifier <code>com.transistorsoft.fetch</code>:</p> <p></p> <p>If you intend to execute your own custom tasks via <code>BackgroundFetch.scheduleTask</code>, you must add those custom identifiers as well.  For example, if you intend to execute a custom <code>taskId: 'com.transistorsoft.customtask'</code>, you must add the identifier <code>com.transistorsoft.customtask</code> to your \"Permitted background task scheduler identifiers\", as well.</p> <p>Warning</p> <p>A task identifier can be any string you wish, but it must be prefixed with <code>com.transistorsoft.</code>.</p>"},{"location":"flutter/setup/#android-setup","title":"Android Setup","text":"<p>No additional Android setup is required for <code>background_fetch &gt;= 1.5.0</code>.</p> <p>Upgrading from &lt; 1.5.0?</p> <p>If you previously added a custom <code>maven</code> URL for <code>background_fetch</code> in your <code>android/build.gradle</code>, you can now remove it.</p>"},{"location":"react-native/","title":"React Native \u2014 Background Fetch","text":"<p>Cross-platform background fetch API for React Native on iOS &amp; Android.</p> <ul> <li> GitHub Repo</li> <li>Setup Guide</li> <li>API Reference \u2014 BackgroundFetch</li> <li>API Reference \u2014 BackgroundFetchConfig</li> <li>API Reference \u2014 TaskConfig</li> </ul>"},{"location":"react-native/BackgroundFetch/","title":"BackgroundFetch","text":""},{"location":"react-native/BackgroundFetch/#configure","title":"configure","text":"<pre><code>static configure(config: BackgroundFetchConfig, onEvent: (taskId: string) =&gt; void, onTimeout?: (taskId: string) =&gt; void): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Configure the plugin and begin listening for background-fetch events.</p> <p>Calling <code>configure</code> automatically starts background-fetch (equivalent to calling BackgroundFetch.start immediately after configuration).</p> <p>The BackgroundFetchConfig.minimumFetchInterval option controls how frequently iOS will wake your app in the background.  Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports additional scheduling constraints such as requiredNetworkType, requiresCharging, etc.</p> <p>Warning</p> <p>You must call BackgroundFetch.finish within your event callback.  Failure to do so will result in the OS throttling future background events for your app.</p> <p>Basic configuration</p> <p><pre><code>const status = await BackgroundFetch.configure({\n  minimumFetchInterval: 15,     // &lt;-- iOS: minimum interval in minutes\n}, async (taskId) =&gt; {\n  console.log('[BackgroundFetch] taskId:', taskId);\n  // You MUST call finish() when your work is complete.\n  BackgroundFetch.finish(taskId);\n}, (taskId) =&gt; {\n  // The OS is about to kill your app \u2014 stop all work immediately.\n  console.log('[BackgroundFetch] TIMEOUT, taskId:', taskId);\n  BackgroundFetch.finish(taskId);\n});\n\nconsole.log('[BackgroundFetch] status:', status);\n</code></pre> Android configuration</p> <pre><code>BackgroundFetch.configure({\n  minimumFetchInterval: 15,\n  stopOnTerminate: false,     // &lt;-- Continue after app termination\n  startOnBoot: true,          // &lt;-- Restart on device reboot\n  enableHeadless: true,       // &lt;-- Enable headless task for terminated app\n  requiresCharging: false,\n  requiresBatteryNotLow: true,\n}, async (taskId) =&gt; {\n  console.log('[BackgroundFetch] taskId:', taskId);\n  BackgroundFetch.finish(taskId);\n}, (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"react-native/BackgroundFetch/#finish","title":"finish","text":"<pre><code>static finish(taskId?: string): void;\n</code></pre> <p>Signal to the OS that your background task is complete.</p> <p>Warning</p> <p>You must call <code>finish</code> at the end of every background-fetch event callback. Failure to call <code>finish</code> has serious consequences:</p> <ul> <li>iOS \u2014 The OS may terminate your app and penalise future background-wake frequency.</li> <li>Android \u2014 The <code>JobScheduler</code> job will not be considered complete and the OS may   throttle or block future executions.</li> </ul> <p>Always call <code>finish</code> as quickly as possible, even when your task encounters an error. For timeout events, call <code>finish</code> immediately without doing any additional work. Example</p> <pre><code>BackgroundFetch.configure({}, async (taskId) =&gt; {\n  try {\n    await doMyWork();\n  } catch (e) {\n    console.warn(e);\n  } finally {\n    // Always call finish, even on error.\n    BackgroundFetch.finish(taskId);\n  }\n}, (taskId) =&gt; {\n  // Timeout \u2014 stop immediately.\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"react-native/BackgroundFetch/#registerheadlesstask","title":"registerHeadlessTask Android only","text":"<pre><code>static registerHeadlessTask(task: (event: HeadlessEvent) =&gt; Promise&lt;void&gt;): void;\n</code></pre> <p>Register a function to execute when the app is terminated while background-fetch events remain active.</p> <p>Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"react-native/BackgroundFetch/#how-it-works","title":"How it works","text":"<p>When Android terminates the app, the native module launches a minimal JS context (without the React Native UI) and invokes your headless task function.  This allows you to handle background events even when the app is not running.</p>"},{"location":"react-native/BackgroundFetch/#registration","title":"Registration","text":"<p>Call <code>registerHeadlessTask</code> at your app's entry point (<code>index.js</code>), outside any React component.  The registration must happen before the OS attempts to fire a headless event.</p>"},{"location":"react-native/BackgroundFetch/#restrictions","title":"Restrictions","text":"<ul> <li>Do not import modules that depend on the React Native UI runtime (e.g. components,   navigation, Redux stores that render UI).</li> <li>Keep the task lightweight \u2014 the OS may kill the JS context at any time.</li> <li>Always call BackgroundFetch.finish before the returned <code>Promise</code> resolves. Example</li> </ul> <pre><code>// index.js \u2014 register at the app entry point.\nimport BackgroundFetch, { HeadlessEvent } from 'react-native-background-fetch';\n\nconst BackgroundFetchHeadlessTask = async (event: HeadlessEvent) =&gt; {\n  const taskId = event.taskId;\n\n  if (event.timeout) {\n    // OS is about to kill the context \u2014 finish immediately.\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n\n  console.log('[BackgroundFetch] Headless event, taskId:', taskId);\n  // ... perform lightweight work ...\n  BackgroundFetch.finish(taskId);\n};\n\nBackgroundFetch.registerHeadlessTask(BackgroundFetchHeadlessTask);\n</code></pre>"},{"location":"react-native/BackgroundFetch/#scheduletask","title":"scheduleTask","text":"<pre><code>static scheduleTask(config: TaskConfig): Promise&lt;boolean&gt;;\n</code></pre> <p>Schedule a custom one-shot or periodic background task in addition to the default fetch event registered with BackgroundFetch.configure.</p> <p>Custom tasks fire the same <code>onEvent</code> callback registered in BackgroundFetch.configure, identified by their TaskConfig.taskId.  Call BackgroundFetch.finish with that <code>taskId</code> to signal completion, and BackgroundFetch.stop with the <code>taskId</code> to cancel.</p>"},{"location":"react-native/BackgroundFetch/#ios","title":"iOS","text":"<p>On iOS, custom tasks are registered using the <code>BGProcessingTask</code> API.  You must declare each <code>taskId</code> in your app's <code>Info.plist</code> under <code>BGTaskSchedulerPermittedIdentifiers</code> before calling <code>scheduleTask</code>.</p>"},{"location":"react-native/BackgroundFetch/#android","title":"Android","text":"<p>Android uses <code>JobScheduler</code> (or <code>AlarmManager</code> when forceAlarmManager is <code>true</code>) and supports all AbstractConfig scheduling constraints. One-shot task</p> <p><pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.customtask',   // &lt;-- unique identifier\n  delay: 5000,                    // &lt;-- minimum delay in ms\n  forceAlarmManager: true,        // &lt;-- Android: bypass JobScheduler\n  stopOnTerminate: false,\n});\n\n// The same onEvent callback from configure() will fire with taskId 'com.foo.customtask'.\n</code></pre> Periodic task</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.periodic-sync',\n  delay: 600000,       // &lt;-- 10 minutes in ms\n  periodic: true,      // &lt;-- repeat indefinitely\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,  // &lt;-- Android\n  requiresNetworkConnectivity: true,  // &lt;-- iOS\n});\n</code></pre>"},{"location":"react-native/BackgroundFetch/#start","title":"start","text":"<pre><code>static start(): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Start subscribing to background-fetch events.</p> <p>BackgroundFetch.configure calls <code>start</code> automatically.  Use <code>start</code> explicitly only after a previous call to BackgroundFetch.stop. Example</p> <pre><code>const status = await BackgroundFetch.start();\nconsole.log('[BackgroundFetch] started, status:', status);\n</code></pre>"},{"location":"react-native/BackgroundFetch/#status","title":"status","text":"<pre><code>static status(callback?: (status: BackgroundFetchStatus) =&gt; void): Promise&lt;BackgroundFetchStatus&gt;;\n</code></pre> <p>Query the current authorization status of the Background Fetch API.</p> Value Constant Description <code>0</code> BackgroundFetchStatus.STATUS_RESTRICTED Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls). <code>1</code> BackgroundFetchStatus.STATUS_DENIED The user explicitly disabled background behavior for this app or the whole system. <code>2</code> BackgroundFetchStatus.STATUS_AVAILABLE Background fetch is available and enabled. Example <pre><code>const status = await BackgroundFetch.status();\nswitch (status) {\n  case BackgroundFetch.STATUS_RESTRICTED:\n    console.log('Background fetch restricted');\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    console.log('Background fetch denied by user');\n    break;\n  case BackgroundFetch.STATUS_AVAILABLE:\n    console.log('Background fetch available');\n    break;\n}\n</code></pre>"},{"location":"react-native/BackgroundFetch/#stop","title":"stop","text":"<pre><code>static stop(taskId?: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Stop subscribing to background-fetch events.</p> <ul> <li>If a <code>taskId</code> is provided, only that specific scheduled task is cancelled.</li> <li>If no <code>taskId</code> is provided, all background-fetch and scheduled tasks are cancelled.</li> </ul> <p>Use BackgroundFetch.start to resume after stopping. Example</p> <pre><code>// Stop all background-fetch events.\nawait BackgroundFetch.stop();\n\n// Stop a single scheduled task by taskId.\nawait BackgroundFetch.stop('com.foo.customtask');\n</code></pre>"},{"location":"react-native/BackgroundFetchConfig/","title":"BackgroundFetchConfig","text":"<p>Configuration object passed to BackgroundFetch.configure.</p>"},{"location":"react-native/BackgroundFetchConfig/#minimumfetchinterval","title":"minimumFetchInterval","text":"<pre><code>minimumFetchInterval?: number;\n</code></pre> <p>The minimum interval in minutes between background-fetch events. Defaults to <code>15</code> minutes.  The minimum allowed value is <code>15</code> minutes.</p> <p>Note</p> <p>The OS does not guarantee fetch events will fire at exactly this interval. iOS adjusts timing based on usage patterns and battery state.  This is a minimum hint, not a precise schedule.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  minimumFetchInterval: 15,   // iOS: fire no more often than every 15 minutes\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"react-native/BackgroundFetchConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"react-native/BackgroundFetchConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Set <code>true</code> to enable React Native's HeadlessJS mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p> <p>\ud83d\udcc2 <code>index.js</code> (MUST be in <code>index.js</code>):</p> <pre><code>import BackgroundFetch from \"react-native-background-fetch\";\n\nconst MyHeadlessTask = async (event) =&gt; {\n  let taskId = event.taskId;\n  let isTimeout = event.timeout;\n  if (isTimeout) {\n    // This task has exceeded its allowed running-time.\n    console.log('[BackgroundFetch] Headless TIMEOUT:', taskId);\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  console.log('[BackgroundFetch HeadlessTask] start:', taskId);\n\n  // Perform your work here...\n\n  BackgroundFetch.finish(taskId);\n};\n\n// Register your BackgroundFetch HeadlessTask\nBackgroundFetch.registerHeadlessTask(MyHeadlessTask);\n</code></pre>"},{"location":"react-native/BackgroundFetchConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>forceAlarmManager?: boolean;\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"react-native/BackgroundFetchConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>requiredNetworkType?: NetworkType;\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n});\n</code></pre>"},{"location":"react-native/BackgroundFetchConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>requiresBatteryNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"react-native/BackgroundFetchConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>requiresCharging?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"react-native/BackgroundFetchConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>requiresDeviceIdle?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"react-native/BackgroundFetchConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>requiresStorageNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"react-native/BackgroundFetchConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"react-native/BackgroundFetchConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"react-native/BackgroundFetchStatus/","title":"BackgroundFetchStatus","text":"<p>Authorization status returned by BackgroundFetch.status and BackgroundFetch.configure.</p>"},{"location":"react-native/BackgroundFetchStatus/#status_available","title":"STATUS_AVAILABLE","text":"<pre><code>static STATUS_AVAILABLE: BackgroundFetchStatus = 2;\n</code></pre> <p>Background fetch is available and enabled.</p>"},{"location":"react-native/BackgroundFetchStatus/#status_denied","title":"STATUS_DENIED","text":"<pre><code>static STATUS_DENIED: BackgroundFetchStatus = 1;\n</code></pre> <p>The user explicitly disabled background behavior for this app or for the whole system.</p>"},{"location":"react-native/BackgroundFetchStatus/#status_restricted","title":"STATUS_RESTRICTED","text":"<pre><code>static STATUS_RESTRICTED: BackgroundFetchStatus = 0;\n</code></pre> <p>Background fetch updates are unavailable and the user cannot enable them again (e.g. parental controls are in effect).</p>"},{"location":"react-native/HeadlessEvent/","title":"HeadlessEvent","text":"<p>The event object delivered to a BackgroundFetch.registerHeadlessTask handler when the app is running in a terminated (headless) state on Android.</p> Property Type Description taskId <code>string</code> Identifier of the task that fired. Pass to BackgroundFetch.finish. timeout <code>boolean</code> <code>true</code> when the OS signals that background time is nearly exhausted. <p>\ud83d\udcc2 <code>index.js</code> (MUST be in <code>index.js</code>):</p> <pre><code>import BackgroundFetch, { HeadlessEvent } from \"react-native-background-fetch\";\n\nconst MyHeadlessTask = async (event: HeadlessEvent) =&gt; {\n  let taskId = event.taskId;\n  let isTimeout = event.timeout;\n  if (isTimeout) {\n    console.log('[BackgroundFetch] Headless TIMEOUT:', taskId);\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  console.log('[BackgroundFetch HeadlessTask] start:', taskId);\n\n  // Perform your work here...\n\n  BackgroundFetch.finish(taskId);\n};\n\n// Register your BackgroundFetch HeadlessTask\nBackgroundFetch.registerHeadlessTask(MyHeadlessTask);\n</code></pre>"},{"location":"react-native/HeadlessEvent/#taskid","title":"taskId","text":"<pre><code>taskId: string;\n</code></pre> <p>The identifier of the task that fired this headless event.</p> <p>Pass this value to BackgroundFetch.finish when your work is complete.</p>"},{"location":"react-native/HeadlessEvent/#timeout","title":"timeout","text":"<pre><code>timeout: boolean;\n</code></pre> <p><code>true</code> when the OS signals that background running-time is nearly exhausted.</p> <p>When a timeout event fires you must immediately call BackgroundFetch.finish and stop any in-progress work.  Failure to do so may cause the OS to penalize your app.</p>"},{"location":"react-native/NetworkType/","title":"NetworkType","text":"<p>Android only Network connectivity constraint for use with requiredNetworkType.</p>"},{"location":"react-native/NetworkType/#network_type_any","title":"NETWORK_TYPE_ANY","text":"<pre><code>static NETWORK_TYPE_ANY: NetworkType = 1;\n</code></pre> <p>Requires any active network connection.</p>"},{"location":"react-native/NetworkType/#network_type_cellular","title":"NETWORK_TYPE_CELLULAR","text":"<pre><code>static NETWORK_TYPE_CELLULAR: NetworkType = 2;\n</code></pre> <p>Requires a cellular (mobile data) network connection.</p>"},{"location":"react-native/NetworkType/#network_type_none","title":"NETWORK_TYPE_NONE","text":"<pre><code>static NETWORK_TYPE_NONE: NetworkType = 0;\n</code></pre> <p>No network constraint \u2014 the task will run regardless of connectivity.  This is the default.</p>"},{"location":"react-native/NetworkType/#network_type_not_roaming","title":"NETWORK_TYPE_NOT_ROAMING","text":"<pre><code>static NETWORK_TYPE_NOT_ROAMING: NetworkType = 4;\n</code></pre> <p>Requires a non-roaming network connection.</p>"},{"location":"react-native/NetworkType/#network_type_unmetered","title":"NETWORK_TYPE_UNMETERED","text":"<pre><code>static NETWORK_TYPE_UNMETERED: NetworkType = 3;\n</code></pre> <p>Requires an unmetered network connection (e.g. Wi-Fi).</p>"},{"location":"react-native/TaskConfig/","title":"TaskConfig","text":"<p>Configuration object passed to BackgroundFetch.scheduleTask.</p>"},{"location":"react-native/TaskConfig/#delay","title":"delay","text":"<pre><code>delay: number;\n</code></pre> <p>The minimum delay in milliseconds before this task fires.</p> <p>The OS may delay the task beyond this value depending on device conditions, battery state, and other scheduling constraints. Example</p> <pre><code>// Fire at least 30 seconds from now.\nBackgroundFetch.scheduleTask({\n  taskId: 'com.foo.task',\n  delay: 30000,\n});\n</code></pre>"},{"location":"react-native/TaskConfig/#periodic","title":"periodic","text":"<pre><code>periodic?: boolean;\n</code></pre> <p>Set <code>true</code> to schedule a repeating task.  Defaults to <code>false</code> (one-shot).</p> <p>When <code>true</code>, the task automatically re-schedules itself after each execution using the same TaskConfig.delay as the repeat interval. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.periodic',\n  delay: 900000,    // 15 minutes in ms\n  periodic: true,\n});\n</code></pre>"},{"location":"react-native/TaskConfig/#requiresnetworkconnectivity","title":"requiresNetworkConnectivity iOS only","text":"<pre><code>requiresNetworkConnectivity?: boolean;\n</code></pre> <p>Set <code>true</code> to require a network connection before this task runs.</p> <p>On Android, use requiredNetworkType instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.networktask',\n  delay: 60000,\n  requiresNetworkConnectivity: true,      // &lt;-- iOS\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY, // &lt;-- Android\n});\n</code></pre>"},{"location":"react-native/TaskConfig/#taskid","title":"taskId","text":"<pre><code>taskId: string;\n</code></pre> <p>A unique identifier for this task.</p> <p>The same <code>taskId</code> is delivered to the <code>onEvent</code> callback registered in BackgroundFetch.configure.  Pass it to BackgroundFetch.finish to signal completion and to BackgroundFetch.stop to cancel the task.</p> <p>Use reverse-domain notation to avoid collisions with other tasks. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.myapp.sync',\n  delay: 60000,\n});\n\n// In your configure() callback:\nBackgroundFetch.configure({}, async (taskId) =&gt; {\n  if (taskId === 'com.myapp.sync') {\n    await performSync();\n  }\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"react-native/TaskConfig/#android-only-constraints","title":"Android-only constraints","text":"<p>The following options apply to Android only.</p>"},{"location":"react-native/TaskConfig/#enableheadless","title":"enableHeadless Android only","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Set <code>true</code> to enable React Native's HeadlessJS mechanism for handling fetch events after app termination.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p> <p>\ud83d\udcc2 <code>index.js</code> (MUST be in <code>index.js</code>):</p> <pre><code>import BackgroundFetch from \"react-native-background-fetch\";\n\nconst MyHeadlessTask = async (event) =&gt; {\n  let taskId = event.taskId;\n  let isTimeout = event.timeout;\n  if (isTimeout) {\n    // This task has exceeded its allowed running-time.\n    console.log('[BackgroundFetch] Headless TIMEOUT:', taskId);\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n  console.log('[BackgroundFetch HeadlessTask] start:', taskId);\n\n  // Perform your work here...\n\n  BackgroundFetch.finish(taskId);\n};\n\n// Register your BackgroundFetch HeadlessTask\nBackgroundFetch.registerHeadlessTask(MyHeadlessTask);\n</code></pre>"},{"location":"react-native/TaskConfig/#forcealarmmanager","title":"forceAlarmManager Android only","text":"<pre><code>forceAlarmManager?: boolean;\n</code></pre> <p>Force the use of Android's <code>AlarmManager</code> API instead of <code>JobScheduler</code>.  Defaults to <code>false</code>.</p> <p>By default the plugin uses <code>JobScheduler</code> on API 21+ and falls back to <code>AlarmManager</code> on older devices.  Set <code>true</code> to force <code>AlarmManager</code> regardless of API level.</p> <p>Note</p> <p><code>AlarmManager</code> bypasses the scheduling optimisations of <code>JobScheduler</code> and is less battery-efficient.  Use it only when <code>JobScheduler</code> does not meet your timing requirements.</p>"},{"location":"react-native/TaskConfig/#requirednetworktype","title":"requiredNetworkType Android only","text":"<pre><code>requiredNetworkType?: NetworkType;\n</code></pre> <p>Specify the kind of network connectivity required to run this task. Defaults to NetworkType.NETWORK_TYPE_NONE (no network required).</p> <p>If the required network type is unavailable, the task will not run until the constraint is satisfied.</p> Value Constant Description <code>0</code> NetworkType.NETWORK_TYPE_NONE No network constraint. <code>1</code> NetworkType.NETWORK_TYPE_ANY Any active network connection. <code>2</code> NetworkType.NETWORK_TYPE_CELLULAR Cellular (mobile data) connection. <code>3</code> NetworkType.NETWORK_TYPE_UNMETERED Unmetered (e.g. Wi-Fi) connection. <code>4</code> NetworkType.NETWORK_TYPE_NOT_ROAMING Non-roaming connection. <p>On iOS, use TaskConfig.requiresNetworkConnectivity instead. Example</p> <pre><code>BackgroundFetch.scheduleTask({\n  taskId: 'com.foo.sync',\n  delay: 60000,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_UNMETERED,\n});\n</code></pre>"},{"location":"react-native/TaskConfig/#requiresbatterynotlow","title":"requiresBatteryNotLow Android only","text":"<pre><code>requiresBatteryNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's battery level to be above the \"low battery\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"react-native/TaskConfig/#requirescharging","title":"requiresCharging Android only","text":"<pre><code>requiresCharging?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be charging (or connected to permanent power) before running this task.  Defaults to <code>false</code>.</p>"},{"location":"react-native/TaskConfig/#requiresdeviceidle","title":"requiresDeviceIdle Android only","text":"<pre><code>requiresDeviceIdle?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device to be idle (not actively used) before running this task.  Defaults to <code>false</code>.</p> <p>This is a good option for resource-intensive tasks.  Battery usage is still attributed to your app and surfaced to the user in battery stats.</p>"},{"location":"react-native/TaskConfig/#requiresstoragenotlow","title":"requiresStorageNotLow Android only","text":"<pre><code>requiresStorageNotLow?: boolean;\n</code></pre> <p>Set <code>true</code> to require the device's available storage to be above the \"low storage\" threshold before running this task.  Defaults to <code>false</code>.</p>"},{"location":"react-native/TaskConfig/#startonboot","title":"startOnBoot Android only","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Set <code>true</code> to resume background-fetch events when the device is rebooted.  Defaults to <code>false</code>.</p> <p>Note</p> <p>Requires stopOnTerminate <code>false</code>.</p>"},{"location":"react-native/TaskConfig/#stoponterminate","title":"stopOnTerminate Android only","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Set <code>false</code> to continue receiving background-fetch events after the user terminates the app.  Defaults to <code>true</code>.</p> <p>Note</p> <p>startOnBoot requires <code>stopOnTerminate: false</code>.</p> <p>Example</p> <pre><code>BackgroundFetch.configure({\n  stopOnTerminate: false,\n  startOnBoot: true,\n  enableHeadless: true,\n}, async (taskId) =&gt; {\n  BackgroundFetch.finish(taskId);\n});\n</code></pre>"},{"location":"react-native/debugging/","title":"Debugging","text":"<p>Background Fetch events are notoriously difficult to test because the OS controls when they fire.  Use these simulation techniques during development.</p>"},{"location":"react-native/debugging/#ios","title":"iOS","text":""},{"location":"react-native/debugging/#simulating-fetch-events","title":"Simulating Fetch Events","text":"<p>iOS 13+ uses the <code>BGTaskScheduler</code> API.  To simulate a fetch event:</p> <ol> <li>Run your app in Xcode.</li> <li>Click the Pause button () to initiate a breakpoint.</li> <li>In the <code>(lldb)</code> console, paste:</li> </ol> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <ol> <li>Click the Continue button ().  The callback provided to <code>configure</code> will receive the event.</li> </ol> <p></p> <p></p> <p></p>"},{"location":"react-native/debugging/#simulating-custom-scheduletask-events","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<p>If you've registered a custom task (e.g. <code>com.transistorsoft.customtask</code>), simulate it with:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateLaunchForTaskWithIdentifier:@\"com.transistorsoft.customtask\"]\n</code></pre>"},{"location":"react-native/debugging/#simulating-task-timeout-events","title":"Simulating Task Timeout Events","text":"<p>To simulate a task timeout, do not call <code>finish(taskId)</code> in your event callback, then run:</p> <pre><code>e -l objc -- (void)[[BGTaskScheduler sharedScheduler] _simulateExpirationForTaskWithIdentifier:@\"com.transistorsoft.fetch\"]\n</code></pre> <p>Your timeout callback will fire, allowing you to verify your timeout handling logic.</p>"},{"location":"react-native/debugging/#ios-tips","title":"iOS Tips","text":"<p>Warning</p> <p>iOS can take hours or even days before Apple's machine-learning algorithm begins regularly firing fetch events.  Do not sit staring at your logs waiting for an event.  If your simulated events work, everything is correctly configured.</p> <ul> <li>If the user doesn't open your app for long periods of time, iOS will stop firing events.</li> <li><code>scheduleTask</code> on iOS seems only to run when the device is plugged into power.</li> <li>There is no <code>stopOnTerminate: false</code> for iOS \u2014 when the app is terminated, iOS stops firing events.</li> </ul>"},{"location":"react-native/debugging/#android","title":"Android","text":""},{"location":"react-native/debugging/#observing-logs","title":"Observing Logs","text":"<p>Use <code>adb logcat</code> to observe plugin logs:</p> <pre><code>adb logcat \"*:S\" TSBackgroundFetch:V\n</code></pre>"},{"location":"react-native/debugging/#simulating-fetch-events_1","title":"Simulating Fetch Events","text":"<p>Simulate a background-fetch event (replace <code>&lt;your.application.id&gt;</code> with your app's package name):</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre>"},{"location":"react-native/debugging/#simulating-custom-scheduletask-events_1","title":"Simulating Custom <code>scheduleTask</code> Events","text":"<ol> <li>Observe <code>adb logcat</code> for the <code>registerTask</code> log entry and copy the <code>jobId</code>:</li> </ol> <pre><code>TSBackgroundFetch: - registerTask: com.your.package.name (jobId: -359368280)\n</code></pre> <ol> <li>Paste the <code>jobId</code> into the <code>adb shell</code> command:</li> </ol> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; -359368280\n</code></pre>"},{"location":"react-native/debugging/#simulating-headless-events","title":"Simulating Headless Events","text":"<p>After terminating your app, simulate a fetch event to test your headless task:</p> <pre><code>adb shell cmd jobscheduler run -f &lt;your.application.id&gt; 999\n</code></pre> <p>Verify the headless task executes by observing <code>adb logcat</code> output.</p>"},{"location":"react-native/examples/","title":"Examples","text":""},{"location":"react-native/examples/#basic-usage","title":"Basic Usage","text":"<pre><code>import BackgroundFetch from \"react-native-background-fetch\";\n\nexport default class App extends React.Component {\n  async componentDidMount() {\n    const status = await BackgroundFetch.configure(\n      {\n        minimumFetchInterval: 15, // minutes\n        stopOnTerminate: false,\n        startOnBoot: true,\n        enableHeadless: true,\n      },\n      async (taskId: string) =&gt; {\n        // &lt;-- Event callback\n        console.log(\"[BackgroundFetch] Event received:\", taskId);\n        // Perform your work here...\n        BackgroundFetch.finish(taskId);\n      },\n      async (taskId: string) =&gt; {\n        // &lt;-- Timeout callback\n        console.log(\"[BackgroundFetch] TIMEOUT:\", taskId);\n        BackgroundFetch.finish(taskId);\n      }\n    );\n    console.log(\"[BackgroundFetch] configure status:\", status);\n  }\n}\n</code></pre>"},{"location":"react-native/examples/#scheduling-a-custom-task","title":"Scheduling a Custom Task","text":"<pre><code>BackgroundFetch.scheduleTask({\n  taskId: \"com.example.my-custom-task\",\n  delay: 60000, // milliseconds\n  periodic: false,\n  forceAlarmManager: true,\n  requiredNetworkType: BackgroundFetch.NETWORK_TYPE_ANY,\n});\n</code></pre> <p>All scheduled tasks fire into the same callback provided to <code>configure</code>.  Use a <code>switch</code> statement to route by <code>taskId</code>:</p> <pre><code>const status = await BackgroundFetch.configure(\n  { minimumFetchInterval: 15 },\n  async (taskId: string) =&gt; {\n    switch (taskId) {\n      case \"com.example.my-custom-task\":\n        console.log(\"Custom task fired\");\n        break;\n      default:\n        console.log(\"Default fetch event\");\n    }\n    BackgroundFetch.finish(taskId);\n  },\n  async (taskId: string) =&gt; {\n    BackgroundFetch.finish(taskId);\n  }\n);\n</code></pre>"},{"location":"react-native/examples/#headless-task-android","title":"Headless Task (Android)","text":"<p>When the app is terminated, Android can continue executing background-fetch events via React Native's HeadlessJS mechanism.</p> <p>Register your headless task in <code>index.js</code> (must be in <code>index.js</code>):</p> <pre><code>import BackgroundFetch, { HeadlessEvent } from \"react-native-background-fetch\";\n\nconst MyHeadlessTask = async (event: HeadlessEvent) =&gt; {\n  const taskId = event.taskId;\n\n  if (event.timeout) {\n    console.log(\"[BackgroundFetch] Headless TIMEOUT:\", taskId);\n    BackgroundFetch.finish(taskId);\n    return;\n  }\n\n  console.log(\"[BackgroundFetch] Headless event:\", taskId);\n  // Perform your work here...\n  BackgroundFetch.finish(taskId);\n};\n\nBackgroundFetch.registerHeadlessTask(MyHeadlessTask);\n</code></pre> <p>Note</p> <p>See enableHeadless for full setup details. Requires stopOnTerminate <code>false</code> and enableHeadless <code>true</code>.</p>"},{"location":"react-native/examples/#checking-status","title":"Checking Status","text":"<pre><code>const status = await BackgroundFetch.status;\n\nswitch (status) {\n  case BackgroundFetch.STATUS_AVAILABLE:\n    console.log(\"Background fetch is available\");\n    break;\n  case BackgroundFetch.STATUS_DENIED:\n    console.log(\"Background fetch is denied by the user\");\n    break;\n  case BackgroundFetch.STATUS_RESTRICTED:\n    console.log(\"Background fetch is restricted (e.g. parental controls)\");\n    break;\n}\n</code></pre>"},{"location":"react-native/examples/#stopping-tasks","title":"Stopping Tasks","text":"<pre><code>// Stop a specific scheduled task\nawait BackgroundFetch.stop(\"com.example.my-custom-task\");\n\n// Stop ALL tasks (including the default fetch event)\nawait BackgroundFetch.stop();\n</code></pre>"},{"location":"react-native/setup/","title":"Setup","text":"React NativeExpo"},{"location":"react-native/setup/#installation","title":"Installation","text":"npmyarn <pre><code>npm install react-native-background-fetch\n</code></pre> <pre><code>yarn add react-native-background-fetch\n</code></pre>"},{"location":"react-native/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"react-native/setup/#cocoapods","title":"CocoaPods","text":"<pre><code>cd ios &amp;&amp; pod install\n</code></pre>"},{"location":"react-native/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your project root \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Background fetch</li> <li>[x] Background processing (only if you intend to use <code>BackgroundFetch.scheduleTask</code>)</li> </ul> <p></p>"},{"location":"react-native/setup/#infoplist","title":"Info.plist","text":"<p>Open your Info.plist and add the key \"Permitted background task scheduler identifiers\":</p> <p></p> <p>Add the required identifier <code>com.transistorsoft.fetch</code>:</p> <p></p> <p>If you intend to execute your own custom tasks via <code>BackgroundFetch.scheduleTask</code>, you must add those custom identifiers as well.  For example, if you intend to execute a custom <code>taskId: 'com.transistorsoft.customtask'</code>, you must add the identifier <code>com.transistorsoft.customtask</code> to your \"Permitted background task scheduler identifiers\", as well.</p> <p>Warning</p> <p>A task identifier can be any string you wish, but it must be prefixed with <code>com.transistorsoft.</code>.</p>"},{"location":"react-native/setup/#android-setup","title":"Android Setup","text":"<p>No Gradle configuration required</p> <p>The native <code>tsbackgroundfetch</code> library is hosted on Maven Central and is resolved automatically.  No custom <code>maven</code> URL or local <code>libs</code> repository is needed.</p>"},{"location":"react-native/setup/#installation_1","title":"Installation","text":"<pre><code>npx expo install react-native-background-fetch\n</code></pre> <p>The plugin ships with an Expo Config Plugin \u2014 it handles all native configuration automatically during <code>expo prebuild</code>.  No manual edits to <code>Info.plist</code>, <code>AppDelegate</code>, or Background Modes are required.</p>"},{"location":"react-native/setup/#appjson","title":"<code>app.json</code>","text":"<p>Add the plugin to <code>plugins</code> and configure <code>ios.infoPlist</code> with the required background modes and task identifiers:</p> <pre><code>{\n  \"expo\": {\n    \"plugins\": [\n      \"react-native-background-fetch\"\n    ],\n    \"ios\": {\n      \"infoPlist\": {\n        \"UIBackgroundModes\": [\n          \"fetch\",\n          \"processing\"\n        ],\n        \"BGTaskSchedulerPermittedIdentifiers\": [\n          \"com.transistorsoft.fetch\"\n        ]\n      }\n    }\n  }\n}\n</code></pre> <p>If you intend to execute your own custom tasks via <code>BackgroundFetch.scheduleTask</code>, you must add those custom identifiers as well to <code>BGTaskSchedulerPermittedIdentifiers</code>.  For example, if you intend to execute a custom <code>taskId: 'com.transistorsoft.customtask'</code>, add the identifier <code>com.transistorsoft.customtask</code>:</p> <pre><code>\"BGTaskSchedulerPermittedIdentifiers\": [\n  \"com.transistorsoft.fetch\",\n  \"com.transistorsoft.customtask\"\n]\n</code></pre> <p>Warning</p> <p>A task identifier can be any string you wish, but it must be prefixed with <code>com.transistorsoft.</code>.</p>"},{"location":"react-native/setup/#prebuild","title":"Prebuild","text":"<p>You must rebuild for the added plugins to be evaluated.</p> <p>If you're developing locally:</p> <pre><code>npx expo prebuild\n</code></pre> <p>If you're using Expo EAS:</p> <pre><code>eas build --profile development --platform android\n</code></pre>"}]}